---
type: "Work Progress"
title: "Spine-cpp parity hardening"
description: "Progress record for autonomous spine-cpp parity refactoring."
tags: ["spine-cpp", "parity", "refactor"]
timestamp: 2026-06-23T00:00:00Z
status: "active"
related_plan: "docs/plans/2026-06-23-001-refactor-spine-cpp-parity-hardening-plan.md"
git_branch: "main"
verified_by: "cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast"
---

# Summary

Autonomous refactoring is active on local `main`. The behavior reference is `spine-cpp` from the pinned latest tag anchor `spine-ts-4.3.8` (`8e12b1250ab88c0f890849ea45aab80338cead63`).

# Verified State

- Full parity gate passed on 2026-06-23 after deleting Rust-only TrackEntry index/internal getters in breaking commit `00469de`: `597 passed, 10 skipped`. Bevy runtime-state export now derives its `animation_index` from `SkeletonData::animation(name)`, while core `TrackEntry` exposes only the C++-style `animation()` getter and keeps `total_alpha` private. Bevy nextest passed with `43 passed, 0 skipped`; Bevy examples, WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check` also passed.
- Full parity gate passed on 2026-06-23 after the TrackEntryHandle read accessor slice: `597 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after deleting Rust-only mix pair query/removal APIs in breaking commit `c5eb82c`: `597 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after deleting track snapshot convenience APIs in breaking commit `9c206bf`: `597 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after deleting track-entry closure convenience APIs in breaking commit `ac9fcb8`: `597 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after deleting the name-based mix getter in breaking commit `472bb75`: `597 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after the state-bound TrackEntryHandle slice: `596 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after the AnimationStateData animation-reference mix accessor slice: `595 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after the AnimationStateData skeleton accessor slice: `594 passed, 10 skipped`.
- Full parity gate passed on 2026-06-23 after the AnimationState track-slots API slice: `593 passed, 10 skipped`.
- U2 cleanup commit `fbc85eb` deleted 634 lines of disabled Skeleton legacy solver code.
- U3 dispatch cleanup commit `73edc54` moved `AnimationState` timeline application onto shared internal dispatch helpers in `animation.rs`.
- U4 parser cleanup commit `48518a5` moved binary animation timeline-order recording onto `TimelineOrderBuilder`; JSON keeps its existing local lookup/order builders.
- U5 settings cleanup commit `e1e827f` moved track entry settings application into the core runtime, replaced the Bevy settings implementation with an alias, and aligned queued delay/mix-duration adjustment with `spine-cpp`.
- U5 field cleanup commit `fc1c241` made `TrackEntry` state private and exposed read-only getters for external tests and Bevy.
- U5 delay cleanup commit `f36cfa7` preserved the `spine-cpp` delay branch shape for handle setters by special-casing negative delay without forcing non-comparable values through the queued-delay formula.
- U5 mix-duration cleanup commit `0c78468` removed Rust-only mix-duration validation from `AnimationStateData` and empty-animation setters so default mix, pair mix, and empty-animation mix durations now use direct C++ assignment semantics.
- U5 empty-animation cleanup commit `0a4204a` made `AnimationState::add_empty_animation` infallible and removed its Rust-only finite-delay guard, matching C++ `addEmptyAnimation`.
- U5 update cleanup commit `f381dc5` removed the Rust-only `AnimationState::update` delta guard and Bevy's scaled-delta clamp so negative time scale reaches the core runtime like C++.
- U5 IK mix cleanup commit `65c078f` removed the Rust-only finite/positive mix guards from one-bone and two-bone IK so negative and NaN mix values flow through the same C++ math path.
- U5 timeline alpha cleanup commit `64679c2` removed Rust-only nonpositive-alpha guards and clamps from `AnimationState` current-entry apply, IK/transform/path/physics/slider timelines, slot color/two-color helpers, and deform timelines, adding negative-alpha characterization coverage for each touched timeline family.
- U5 TrackEntry mixBlend cleanup commit `1a432d3` replaced the Rust-only public additive surface with official C++ `TrackEntry::mixBlend`: core getters/setters/settings/snapshots, Bevy runtime state, render scenario commands, oracle tests, smoke tests, and `pose_dump_scenario --entry-mix-blend` now use `MixBlend`.
- U5 TrackEntry holdPrevious cleanup commit `e25ca6e` restored official C++ `TrackEntry::getHoldPrevious/setHoldPrevious`: Rust/Bevy/oracle surfaces now expose `hold_previous`/`--entry-hold-previous`, and the focused/full gates remain green.
- U5 TrackEntry query-helper commit `866b732` exposed official-style `is_complete`, `was_applied`, `is_empty_animation`, `set_loop`, and settings-level `with_looped`.
- U5 TrackEntry queue-neighbor commit `8c074f6` exposed safe-handle equivalents for C++ `getPrevious`, `getNext`, and `isNextReady`.
- U5 TrackEntry link/time-setter commit `cfd01fe` stores previous/next links explicitly, exposes `mixing_from`/`mixing_to`, and adds official-style `set_track_time`/`set_mix_time`.
- U5 AnimationState apply-status commit `d39573a` changed `AnimationState::apply` to return the official C++ boolean status for whether any non-delayed track was applied.
- U5 AnimationState accessor commit `8a46380` added safe Rust equivalents for C++ `getCurrent` and `getData`.
- U5 AnimationState queue-control commit `f6b88fe` exposed `disable_queue` and `enable_queue`, matching C++ event-queue drain gating.
- U5 manual TrackEntry disposal commit `d0ada6c` added optional manual disposal controls matching C++ `setManualTrackEntryDisposal`, `getManualTrackEntryDisposal`, and `disposeTrackEntry`.
- U5 AnimationState tracks-view commit `ca658f2` added a sparse current-track slot view matching the shape of C++ `getTracks()`.
- U5 AnimationStateData skeleton accessor commit `c2147b3` hid the public `Arc<SkeletonData>` field and added a C++-style `skeleton_data()` getter matching `getSkeletonData()`.
- U5 AnimationStateData animation-reference mix accessor commit `1f4dd8c` added safe equivalents for C++ `setMix(Animation&, Animation&)` and `getMix(Animation&, Animation&)`.
- U5 state-bound TrackEntryHandle commit `c30b446` added owner identity to safe entry handles, preventing cross-state handle misuse and covering the safe Rust equivalent of C++ `TrackEntry::getAnimationState()`.
- U5 TrackEntryHandle read accessor commit `3710686` added `with_entry` so current and queued handles returned from state methods can read `TrackEntry` getters directly while preserving owner checks.
- U5 mix API cleanup commit `c5eb82c` removed Rust-only `AnimationStateData::pair_mix` / `remove_mix` and Bevy `SpineAnimationCommand::remove_mix` / `RemoveMix`, leaving mix table control on the official C++-style `setMix` / `getMix` / `clear` path.
- U5 snapshot cleanup commit `9c206bf` removed Rust-only `AnimationState::tracks_len` and `AnimationState::track_snapshots`; Bevy runtime-state sync now uses `tracks()` and state-bound entry reads instead of a batch snapshot helper.
- U5 track-entry helper cleanup commit `ac9fcb8` removed Rust-only `AnimationState::with_track_entry` and `with_queued_track_entry`; tests and Bevy helpers now read entries through `current()`, `TrackEntryHandle::next()`, and `TrackEntryHandle::with_entry()`.
- U5 AnimationStateData getter cleanup commit `472bb75` removed Rust-only `get_mix(&str, &str)`; public mix reads now use the official C++-style `get_mix_animation` overload.
- U5 TrackEntry public getter cleanup commit `00469de` removed Rust-only `animation_index()` and `total_alpha()` getters. Internal mix accounting still uses `total_alpha`, and Bevy derives public runtime-state animation indices through `SkeletonData::animation(name)`.
- U6 path scratch commit `3edaa0b` moved path constraint scratch storage and capacity estimation into private `skeleton::path`.
- U6 path world-position commit `0dab0fb` moved path attachment lookup, `compute_path_world_positions`, and private path curve helpers into private `skeleton::path`.
- U6 update-cache commit `190a119` moved cache ordering helpers into private `skeleton::cache`.
- U6 bone transform commit `757b2f7` moved BonePose-equivalent world/local transform helpers and root/child world-transform math into private `skeleton::bone`.
- U6 applied-transform commit `a37abac` moved BonePose-equivalent `modifyWorld`, `modifyLocal`, child world reset, and applied-transform decomposition into private `skeleton::bone`.
- U6 bone world-update commit `fc3ef3c` moved the bone world-transform update entry into private `skeleton::bone`.
- U6 IK commit `e076419` moved the IK solver entry and helper routines into private `skeleton::ik`.
- U6 transform commit `d772a9f` moved the transform constraint solver entry and helper routines into private `skeleton::transform`.
- U6 physics commit `6be2f7b` moved the physics constraint solver entry and helper routines into private `skeleton::physics`.
- U6 slider commit `6104586` moved the slider constraint solver entry and helper routines into private `skeleton::slider`.
- U6 path apply commit `5e93794` moved the path constraint apply entry into private `skeleton::path` and narrowed path-only helper visibility.
- U6 vertices commit `7f98a3d` moved generic attachment world-vertices computation into private `skeleton::vertices`, closer to official `VertexAttachment::computeWorldVertices`.
- U6 Bone type commit `b712f53` moved the `Bone` runtime type into private `skeleton::bone` and re-exported it from `skeleton`.
- U6 Slot type commit `6f56a26` moved the `Slot` runtime type into private `skeleton::slot` and re-exported it from `skeleton`.
- U6 constraint type commit `fcf3389` moved IK, transform, path, physics, and slider runtime constraint types into their matching private modules and re-exported them from `skeleton`.
- U6 Skeleton field hardening commit `047be09` made `Skeleton` container/state fields crate-visible and added public collection accessors plus official-style color, position, and scale setters.
- U6 Bone field hardening commit `12218d2` made `Bone` local pose, applied pose, active state, and world-transform fields crate-visible and added public accessors/setters matching the official `BoneLocal`/`BonePose` shape.
- U6 Slot field hardening commit `2643dd0` made `Slot` pose fields crate-visible and added public accessors/setters matching official `SlotPose`, including attachment-change deform/sequence reset.
- U6 constraint field hardening commit `0c8d8cd` made IK, transform, path, physics, and slider runtime pose fields crate-visible and added public accessors/setters matching the official C++ constraint pose/control shape.
- U6 physics movement controls commit `ecdf83f` added `Skeleton::physics_translate`, `Skeleton::physics_rotate`, `PhysicsConstraint::translate`, and `PhysicsConstraint::rotate`, matching the official C++ API shape and formulas.
- U6 named lookup helpers commit `9bae119` added root/find bone/slot helpers, slot-name attachment lookup, and source-skin-aware `set_attachment` semantics matching the official no-op-on-miss behavior.
- U6 bounds helper commit `83df693` added a no-clipper `Skeleton::bounds` helper that computes an official-style AABB over active region and mesh attachments in draw order.
- U6 constraint lookup helpers commit `fed0975` added explicit by-name find/index/mut helpers for IK, transform, path, physics, and slider constraints.
- U6 setup pose split commit `955cc27` added official-style `Skeleton::setup_pose`, `setup_pose_bones`, and `setup_pose_slots`, and corrected empty setup attachment `sequenceIndex` reset behavior to match `spine-cpp`.
- U6 setup pose alias cleanup commit `c385349` removed the legacy `Skeleton::set_to_setup_pose` alias as a breaking API cleanup and migrated internal tests, examples, Bevy, WGPU examples, and web call sites to `setup_pose`.
- U6 clipping-aware bounds commit `43c5503` added `Skeleton::bounds_with_clipping`, mirroring the official `getBounds(..., SkeletonClipping*)` overload while leaving `bounds()` as the default no-clipper overload.
- U6 world/skin control cleanup commit `aec70e4` aligned Skeleton setter semantics with `spine-cpp`: wind/gravity/time/update are direct assignments, component wind/gravity accessors exist, missing skins no-op, and `Error::UnknownSkin` was deleted.
- U6 skin-result cleanup commit `ae1ab99` removed the useless `Skeleton::set_skin` `Result` wrapper; callers now use the no-return setter directly.
- U6 skin accessor cleanup commit `ea3d166` changed `Skeleton::skin()` to return `Option<&SkinData>` like C++ `getSkin()` and added `skin_name()` for the stored name.
- U6 update-cache public-surface commit `f0903f1` exposed `UpdateCacheItem` and `Skeleton::update_cache_items()` as a read-only typed view over the solver cache, matching C++ `getUpdateCache()` without mutable cache access.
- U6 ordered constraint public-surface commit `295c836` exposed `ConstraintRef` and `Skeleton::constraints()` as a read-only typed view sorted by global constraint order, covering the Rust equivalent of C++ `getConstraints()`.
- U6 BonePose helper public-surface commit `c20ab80` exposed `Bone::child_indices`, parent-space point transforms, local/world rotation transforms, and `rotate_world`, covering the remaining read-only/math helper surface from C++ `Bone`/`BonePose`.
- U6 BonePose update/local public-surface commit `b2cadd4` exposed single-bone world/local transform update, validation, and modification marker helpers through `Skeleton`, covering C++ `BonePose` update/cache helpers without exposing the raw update counter.
- U6 BonePose local-update epoch fix commit `e3e96c0` made public local-transform updates keep the world epoch current, matching C++ `BonePose::updateLocalTransform`.
- U6 y-down scale control commit `d374ddf` exposed `Bone::is_y_down/set_y_down` and routed `Skeleton::scale_y()` plus BonePose transform math through the effective C++ scaleY while preserving the default Y-up baseline.
- U6 update-cache debug cleanup commit `71ddc60` removed hidden `Skeleton::debug_update_cache`; debug callers now format `update_cache_items()` locally.
- U6 attachment world-vertices API cleanup commit `9ea3c43` replaced hidden `slot_vertex_attachment_world_vertices` with documented `slot_attachment_world_vertices`.
- U6 draw-order split commit `26c4709` separated applied draw order from the unconstrained pose and routed slider draw-order timelines through the applied buffer.
- U6 slot-pose split commit `9147966` separated applied slot pose from the unconstrained pose and routed slider-driven slot, attachment, sequence, and deform timelines through the applied buffer when C++ `appliedPose=true` applies.
- U6 slot API cleanup commit `068263d` removed the Rust-only `Slot::set_attachment_name`; name-based changes now use `Skeleton::set_attachment`, the API that can resolve skins and apply C++-style deform/sequence reset semantics.
- U6 slot API cleanup commit `7b83c3c` removed the Rust-only `Slot::set_bone_index`; external code can inspect the slot's bone index but cannot rebind slots at runtime through a non-C++ API.
- U6 slot data cleanup commit `9afff8b` removed runtime `Slot` blend storage; draw extraction now reads `SlotData.blend`, matching C++ `SlotData::getBlendMode()`.
- U6 attachment deform cleanup commit `4f8b351` centralized source-skin and timeline-attachment comparison so public `Skeleton::set_attachment`, `set_skin` attachAll paths, and animation attachment timelines all preserve deform for linked meshes sharing a timeline attachment.
- U6 skin helper commit `b3ca6c1` added official-style `SkinData` attachment helpers and moved `add_skin` attachment merging through the same growable set path.
- U6 skeleton API cleanup commit `7cd8d8c` removed the Rust-only `Skeleton::set_attachment` success return so the method matches C++ void/no-op-on-miss semantics.
- Post-U2 verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`544 passed, 10 skipped`)
- Post-U3 verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state animation --no-fail-fast` (`76 passed, 478 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`544 passed, 10 skipped`)
- Post-U4 verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke timeline_order --no-fail-fast` (`5 passed, 549 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`544 passed, 10 skipped`)
- Post-U5 settings-slice verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state animation --no-fail-fast` (`78 passed, 478 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-bevy`
- Post-U5 field/delay-slice verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state animation --no-fail-fast` (`78 passed, 478 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-bevy`
- Post-U5 empty-animation cleanup verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke add_empty_animation set_empty_animation set_empty_animations_stores_mix_duration_directly animation_state_data_default_mix_is_directly_stored_and_used_as_fallback animation_state_data_rejects_unknown_animations --no-fail-fast --status-level fail` (`8 passed, 576 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`574 passed, 10 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U5 update cleanup verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke update_accepts_negative_delta_like_cpp state_time_scale_scales_update_and_queue_progression animation_state --no-fail-fast --status-level fail` (`57 passed, 528 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast update_respects_negative_animation_time_scale update_writes_spine_animation_event_messages animation_component_change_writes_control_messages --status-level fail` (`3 passed, 40 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`575 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U5 IK mix cleanup verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke ik_one_bone_negative_mix_rotates_away_from_target ik_one_bone_nan_mix_propagates_nan_rotation ik_one_bone_rotates_toward_target ik_two_bones_moves_end_effector_close_to_target ik_timeline_mix_zero_disables_constraint_effect --no-fail-fast --status-level fail` (`5 passed, 582 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`577 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U5 timeline alpha cleanup verification passed:
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke negative_alpha slot_color_timeline_applies_negative_alpha_like_cpp slot_rgba2_timeline_applies_negative_alpha_to_dark_color_like_cpp --no-fail-fast --status-level fail` (`8 passed, 589 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`587 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U5 TrackEntry mixBlend cleanup verification passed:
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_mixing_semantics_tests` (`8 passed, 586 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::oracle_scenario_parity_tests` (`312 passed, 282 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke` (`584 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo fmt --all`
  - `git diff --check`
- Post-U5 oracle toolchain verification passed:
  - `python3 -m py_compile scripts/record_oracle_goldens.py scripts/record_oracle_render_goldens.py`
  - `zsh -n scripts/run_spine_cpp_lite_oracle.zsh scripts/run_spine_cpp_lite_render_oracle.zsh scripts/run_spine_cpp_lite_dump_constraints.zsh`
  - `python3 -c 'import sys; sys.path.insert(0,"scripts"); import record_oracle_goldens as r; print(r.parse_commands("entry.set_mix_blend(&mut state, crate::MixBlend::Add);"))'` -> `['--entry-mix-blend', 'add']`
  - `SPINE2D_ORACLE_ALLOW_BASELINE_MISMATCH=1 scripts/run_spine_cpp_lite_oracle.zsh ... --entry-mix-blend add --step 0.1`
  - `SPINE2D_ORACLE_ALLOW_BASELINE_MISMATCH=1 scripts/run_spine_cpp_lite_render_oracle.zsh ... --entry-mix-blend add --entry-alpha 0.5 --step 0.3`
  - both temp outputs passed `python3 -m json.tool`
  - commit `9bb858f`
- Post-U5 TrackEntry holdPrevious cleanup verification passed:
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_mixing_semantics_tests --no-fail-fast` (`10 passed, 586 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-bevy --examples`
  - `python3 -m py_compile scripts/record_oracle_goldens.py`
  - `zsh -n scripts/run_spine_cpp_lite_oracle.zsh scripts/run_spine_cpp_lite_render_oracle.zsh`
  - parser smoke for `entry.set_hold_previous(&mut state, true);` -> `['--entry-hold-previous', '1']`
  - `SPINE2D_ORACLE_ALLOW_BASELINE_MISMATCH=1 scripts/run_spine_cpp_lite_oracle.zsh ... --entry-hold-previous 1 --step 0.1`
  - `SPINE2D_ORACLE_ALLOW_BASELINE_MISMATCH=1 scripts/run_spine_cpp_lite_render_oracle.zsh ... --entry-hold-previous 1 --step 0.1`
  - both temp outputs passed `python3 -m json.tool`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`586 passed, 10 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
  - commit `e25ca6e`
- Post-U5 TrackEntry query-helper API verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke track_entry_official_query_helpers_and_loop_setter_follow_cpp_state --no-fail-fast --status-level fail` (`1 passed, 596 skipped`)
  - `cargo nextest run -p spine2d-bevy set_animation_command_applies_track_entry_settings_to_current_entry --no-fail-fast --status-level fail` (`1 passed, 42 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state --no-fail-fast --status-level fail` (`59 passed, 538 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`587 passed, 10 skipped`)
  - `git diff --check`
  - commit `866b732`
- Post-U5 TrackEntry queue-neighbor API verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke track_entry_queue_neighbors_follow_cpp_previous_next_chain --no-fail-fast --status-level fail` (`1 passed, 597 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`588 passed, 10 skipped`)
  - `git diff --check`
  - commit `8c074f6`
- Post-U5 TrackEntry link/time-setter API verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke track_entry_official_query_helpers_and_loop_setter_follow_cpp_state track_entry_queue_neighbors_follow_cpp_previous_next_chain --no-fail-fast --status-level fail` (`2 passed, 596 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`588 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `git diff --check`
  - commit `cfd01fe`
- Post-U5 AnimationState public-surface verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_apply_returns_whether_any_track_was_applied --no-fail-fast --status-level fail` (`1 passed, 598 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_current_returns_current_track_handle animation_state_data_accessor_matches_mutable_data --no-fail-fast --status-level fail` (`2 passed, 599 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_queue_can_be_disabled_until_next_drain_point animation_state_current_returns_current_track_handle animation_state_data_accessor_matches_mutable_data --no-fail-fast --status-level fail` (`3 passed, 599 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_manual_track_entry_disposal_matches_cpp_lifetime_control animation_state_queue_can_be_disabled_until_next_drain_point --no-fail-fast --status-level fail` (`2 passed, 601 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_current_returns_current_track_handle --no-fail-fast --status-level fail` (`1 passed, 602 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`593 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `git diff --check`
- Post-U5 AnimationStateData mix API cleanup verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::animation_state_data_named_mix_overrides_default_mix runtime::animation_state_tests::animation_state_data_clear_resets_default_and_animation_mixes runtime::animation_state_tests::animation_state_data_rejects_unknown_animations --no-fail-fast --status-level fail` (`3 passed, 604 skipped`)
  - `cargo nextest run -p spine2d-bevy mix_set_and_clear_commands_update_state_data --no-fail-fast --status-level fail` (`1 passed, 42 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`597 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `cargo check -p spine2d-bevy --examples`
  - `git diff --check`
  - commits `d39573a`, `8a46380`, `f6b88fe`, `d0ada6c`, `ca658f2`
- Post-U5 track snapshot cleanup verification passed:
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`597 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-bevy --examples`
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `cargo fmt --all --check`
  - `git diff --check`
  - commit `9c206bf`
- Post-U5 track-entry helper cleanup verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`597 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-bevy --examples`
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `cargo fmt --all --check`
  - `git diff --check`
  - commit `ac9fcb8`
- Post-U5 AnimationStateData name-getter cleanup verification passed:
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_data_default_mix_is_directly_stored_and_used_as_fallback animation_state_data_named_mix_overrides_default_mix animation_state_data_animation_mix_accessors_match_name_mix_storage animation_state_data_clear_resets_default_and_animation_mixes animation_state_data_rejects_unknown_animations --no-fail-fast --status-level fail` (`5 passed, 602 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`597 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-bevy --examples`
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `cargo fmt --all --check`
  - `git diff --check`
  - commit `472bb75`
- Post-U5 AnimationStateData skeleton accessor verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::animation_state_data_skeleton_data_accessor_exposes_bound_skeleton --no-fail-fast --status-level fail` (`1 passed, 603 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`594 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `git diff --check`
  - commit `c2147b3`
- Post-U5 AnimationStateData animation-reference mix accessor verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::animation_state_data_animation_mix_accessors_match_name_mix_storage --no-fail-fast --status-level fail` (`1 passed, 604 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`595 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `git diff --check`
  - commit `1f4dd8c`
- Post-U5 state-bound TrackEntryHandle verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::track_entry_handles_are_bound_to_their_animation_state --no-fail-fast --status-level fail` (`1 passed, 605 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`596 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `git diff --check`
  - commit `c30b446`
- Post-U5 TrackEntryHandle read accessor verification passed:
  - `cargo fmt --all --check`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::track_entry_handle_reads_current_and_queued_entries --no-fail-fast --status-level fail` (`1 passed, 606 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`597 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu -p spine2d-web`
  - `cargo fmt --all --check`
  - `git diff --check`
  - commit `3710686`
- Additive API rollback cleanup:
  - User confirmed the remaining dirty files were safe to handle.
  - The affected Rust/Bevy working-tree edits were restored to the committed `mixBlend` public API state from `1a432d3`.
  - `git status --short` is clean before continuing runtime edits.
- Post-U6 path-scratch verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton path_constraint transform_constraint ik physics slider --no-fail-fast` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 path-world-helper verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton path_constraint transform_constraint ik physics slider --no-fail-fast` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 update-cache verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton skin_active path_constraint transform_constraint ik physics slider --no-fail-fast` (`117 passed, 439 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 bone-transform verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton upstream_ik_demo ik path_constraint transform_constraint physics slider --no-fail-fast` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 applied-transform verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton upstream_ik_demo ik path_constraint transform_constraint physics slider --no-fail-fast` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 bone-world-update verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton upstream_ik_demo ik path_constraint transform_constraint physics slider --no-fail-fast` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 IK verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke ik upstream_ik_demo skeleton transform_constraint path_constraint physics slider --no-fail-fast` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`546 passed, 10 skipped`)
- Post-U6 transform verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke transform_constraint skeleton ik path_constraint physics slider --no-fail-fast --status-level fail` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 physics verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke physics skeleton ik path_constraint transform_constraint slider --no-fail-fast --status-level fail` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 slider verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke slider animation animation_state skeleton ik path_constraint transform_constraint physics --no-fail-fast --status-level fail` (`186 passed, 370 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 path-apply verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke path_constraint skeleton ik transform_constraint physics slider upstream_ik_demo --no-fail-fast --status-level fail` (`112 passed, 444 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 vertices verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke world_vertices path_constraint deform render attachment sequence skeleton --no-fail-fast --status-level fail` (`166 passed, 390 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 Bone type verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bone upstream_ik_demo ik path_constraint transform_constraint physics slider --no-fail-fast --status-level fail` (`119 passed, 437 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 Slot type verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke slot attachment render deform sequence skeleton path_constraint --no-fail-fast --status-level fail` (`173 passed, 383 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 constraint type verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bone slot ik transform_constraint path_constraint physics slider world_vertices attachment render sequence --no-fail-fast --status-level fail` (`195 passed, 361 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`546 passed, 10 skipped`)
- Post-U6 Skeleton field hardening verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bone slot ik transform_constraint path_constraint physics slider world_vertices attachment render sequence --no-fail-fast --status-level fail` (`196 passed, 361 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`547 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 Bone field hardening verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bone slot ik transform_constraint path_constraint physics slider world_vertices attachment render sequence --no-fail-fast --status-level fail` (`197 passed, 361 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`548 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 Slot field hardening verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bone slot ik transform_constraint path_constraint physics slider world_vertices attachment render sequence --no-fail-fast --status-level fail` (`198 passed, 361 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`549 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 constraint field hardening verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bone slot ik transform_constraint path_constraint physics slider world_vertices attachment render sequence --no-fail-fast --status-level fail` (`199 passed, 361 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`550 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 physics movement controls verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton physics --no-fail-fast --status-level fail` (`85 passed, 476 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`551 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 named lookup helper verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton --no-fail-fast --status-level fail` (`15 passed, 549 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`554 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 bounds helper verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton --no-fail-fast --status-level fail` (`16 passed, 549 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`555 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 constraint lookup helper verification passed:
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton --no-fail-fast --status-level fail` (`17 passed, 549 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`556 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo fmt --all --check`
  - `git diff --check`
- Post-U6 setup pose split verification passed:
  - `git ls-remote --tags https://github.com/EsotericSoftware/spine-runtimes.git 'refs/tags/spine-ts-4.3.*' | tail -n 10` still reports `spine-ts-4.3.8` as the latest 4.3 anchor.
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton --no-fail-fast --status-level fail` (`18 passed, 549 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`557 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
- Post-U6 setup pose alias cleanup verification passed:
  - `rg -n "\bset_to_setup_pose\b" -g '*.rs'` returned no Rust code matches.
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton --no-fail-fast --status-level fail` (`18 passed, 549 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`557 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check --workspace --examples` was attempted but failed in external `naga v27.0.3` with `WriteColor` trait errors before validating this crate's examples; the targeted WGPU examples gate above passed with the required `json` feature.
- Post-U6 clipping-aware bounds verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton bounds clipping --no-fail-fast --status-level fail` (`29 passed, 539 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`558 passed, 10 skipped`)
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
- Post-U6 world/skin control cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_world_controls skeleton_attachment_lookup_prefers_current_skin_then_default_skin skin_active deform render_oracle --no-fail-fast --status-level fail` (`86 passed, 482 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`558 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 skin accessor cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_attachment_lookup_prefers_current_skin_then_default_skin skeleton_world_controls --no-fail-fast --status-level fail` (`2 passed, 566 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`558 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 update-cache public-surface verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_update_cache_items_expose_read_only_solver_order skeleton_accessors --no-fail-fast --status-level fail` (`2 passed, 567 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`559 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 ordered constraint public-surface verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_constraints_expose_ordered_typed_constraint_refs skeleton_update_cache_items_expose_read_only_solver_order --no-fail-fast --status-level fail` (`2 passed, 568 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`560 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 BonePose helper public-surface verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke bone_child_indices_expose_skeleton_hierarchy bone_parent_space_helpers_follow_parent_world_transform bone_rotation_helpers_match_bone_pose_formulas --no-fail-fast --status-level fail` (`3 passed, 570 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`563 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 BonePose update/local public-surface verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_bone_world_transform_helper_recomputes_modified_local_pose skeleton_bone_local_transform_helper_rebuilds_applied_pose_from_world skeleton_validate_bone_local_transform_uses_world_modified_marker --no-fail-fast --status-level fail` (`3 passed, 573 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`566 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
  - `e3e96c0` additionally strengthened the local-transform test so same-epoch world transforms are not clobbered after local validation.
- Post-U6 y-down scale control verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke bone_y_down_switch_controls_skeleton_scale_y --no-fail-fast --status-level fail` (`1 passed, 576 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`567 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 update-cache debug cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_update_cache_items_expose_read_only_solver_order --no-fail-fast --status-level fail` (`1 passed, 576 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`567 passed, 10 skipped`)
- Post-U6 attachment world-vertices API cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke world_vertices bounds clipping --no-fail-fast --status-level fail` (`12 passed, 565 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`567 passed, 10 skipped`)
- Post-U6 draw-order split verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke slider_draw_order_uses_applied_buffer_without_mutating_pose draw_order bounds clipping --no-fail-fast --status-level fail` (`45 passed, 533 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`568 passed, 10 skipped`)
- Post-U6 slot-pose split verification passed:
  - `cargo fmt --all --check`
  - `git diff --check`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke slider_slot_pose_uses_applied_buffer_without_mutating_pose slider_draw_order_uses_applied_buffer_without_mutating_pose slot_timeline color_timeline sequence_timeline deform skeleton examples_pose_parity_tests oracle_scenario_parity_tests --no-fail-fast --status-level fail` (`363 passed, 216 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`569 passed, 10 skipped`)
  - `cargo check -p spine2d-bevy`
  - `cargo check -p spine2d-bevy --examples`
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`42 passed, 0 skipped`)
  - `cargo check -p spine2d-wgpu`
  - `cargo check -p spine2d-wgpu --examples --features json`
  - `cargo check -p spine2d-web`
- Post-U6 slot API cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check -- spine2d/src/runtime/skeleton/slot.rs spine2d/src/runtime/skeleton_tests.rs`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke slot_accessors_expose_attachment_tint_and_deform_state skeleton_setup_pose_methods_match_cpp_split slot --no-fail-fast --status-level fail` (`22 passed, 557 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`569 passed, 10 skipped`)
- Post-U6 slot bone-index cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check -- spine2d/src/runtime/skeleton/slot.rs spine2d/src/runtime/skeleton_tests.rs`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke slot_accessors_expose_attachment_tint_and_deform_state skeleton_named_lookup_helpers_follow_cpp_convenience_shape slot --no-fail-fast --status-level fail` (`21 passed, 558 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`569 passed, 10 skipped`)
- Post-U6 slot blend cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check -- spine2d/src/runtime/skeleton/slot.rs spine2d/src/runtime/skeleton.rs spine2d/src/render.rs spine2d/src/runtime/skeleton_tests.rs`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_setup_pose_methods_match_cpp_split slot_accessors_expose_attachment_tint_and_deform_state build_draw_list_splits_draws_by_blend_mode render --no-fail-fast --status-level fail` (`37 passed, 542 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`569 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
- Post-U6 attachment deform cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check -- spine2d/src/runtime/skeleton.rs spine2d/src/runtime/animation.rs spine2d/src/runtime/skeleton_tests.rs`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_set_attachment_preserves_deform_for_matching_timeline_attachment skeleton_set_skin_preserves_deform_for_matching_timeline_attachment skeleton_set_skin_from_default_preserves_deform_for_matching_timeline_attachment skeleton_set_attachment_updates_source_skin_and_pose_state attachment_switch_between_linked_mesh_and_parent_preserves_deform_when_timeline_attachment_matches deform --no-fail-fast --status-level fail` (`79 passed, 503 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`572 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
- Post-U6 skin helper verification passed:
  - `cargo fmt --all --check`
  - `git diff --check -- spine2d/src/model.rs spine2d/src/runtime/skin_active_semantics_tests.rs`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke add_skin_is_idempotent_for_lists_and_last_write_wins_for_attachments skin_attachment_iteration_preserves_insertion_order skin_set_attachment_grows_slot_storage_and_remove_is_noop_on_missing_entries skin_active_semantics --no-fail-fast --status-level fail` (`6 passed, 577 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`573 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
- Post-U6 skeleton attachment return cleanup verification passed:
  - `cargo fmt --all --check`
  - `git diff --check -- spine2d/src/runtime/skeleton.rs spine2d/src/runtime/skeleton_tests.rs`
  - `cargo check -p spine2d --features json,binary,upstream-smoke`
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_set_attachment_updates_source_skin_and_pose_state skeleton_set_attachment_preserves_deform_for_matching_timeline_attachment skeleton_named_lookup_helpers_follow_cpp_convenience_shape skeleton --no-fail-fast --status-level fail` (`33 passed, 550 skipped`)
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`573 passed, 10 skipped`)
  - `cargo check -p spine2d --examples --features json,binary,upstream-smoke`
- The worktree was clean before creating the hardening plan and memory updates.
- Existing golden metadata is intentionally not rewritten unless assets or oracle outputs are regenerated.

# In Progress

- U1 is complete: the hardening plan and engineering memory baseline are recorded.
- U2 is complete: disabled `#[cfg(any())]` Skeleton legacy code has been removed.
- U3 is complete: timeline dispatch is centralized behind internal runtime/state helpers, while `AnimationState` keeps only policy decisions for alpha, hold, additive, thresholds, and draw-order output.
- U4 is complete: binary parser timeline-order ownership is centralized behind `TimelineOrderBuilder`, and JSON already had explicit local lookup/order builders.
- U5 is complete: the shared `TrackEntrySettings` value object is now owned by the core runtime and used by Bevy, direct `TrackEntry` field exposure has been removed, delay setter branches now follow the official C++ shape, mix-duration / empty-animation setters follow direct assignment, `AnimationState::update` now allows reverse/non-finite delta flow like C++, IK mix propagation follows the same C++ math path for negative and NaN mix values, timeline alpha now propagates through C++-style negative/nonpositive paths for state, constraint, slot color, slider, and deform timelines, the Rust-only `MixInterpolation` extension has been deleted, mix/threshold boundary comparisons now use the same direct comparison shape as `spine-cpp`, the public TrackEntry blend API has been restored to official C++ `mixBlend` while removing the stale Rust-only additive public surface, `TrackEntryHandle` now carries owning `AnimationState` identity and exposes safe `animation_state` plus `with_entry` accessors, the public AnimationState surface now covers C++ apply status, current/data/tracks accessors, event queue controls, and manual TrackEntry disposal, and `AnimationStateData` now exposes its skeleton through a C++-style getter plus animation-reference mix accessors while deleting the Rust-only single-pair mix query/removal API, name-based mix getter, track snapshot convenience APIs, and closure-based track-entry read helpers.
- U6 is in progress: path constraint scratch storage, capacity estimation, path attachment lookup, path world-position calculation, path apply, and private path curve helpers have moved into `skeleton::path`; update-cache ordering and read-only public cache inspection have moved into `skeleton::cache`; the old hidden `debug_update_cache` string helper has been removed; `Bone` plus BonePose-equivalent world/local transform helpers, root/child world-transform math, `modifyWorld`, `modifyLocal`, child reset, applied-transform decomposition, and the bone world-update entry have moved into `skeleton::bone`; `Bone` now also exposes C++-style child-index, parent-space point, local/world rotation, world-matrix rotation, and y-down scale controls; `Skeleton` exposes C++-style single-bone BonePose update/local validation and modification marker helpers without the raw update counter, and its world/local transform math honors the effective C++ scaleY; draw order now has a split applied buffer plus unconstrained pose accessors, and slider draw-order timelines target the applied buffer during world transforms; `Slot` has moved into `skeleton::slot`, slot pose now has a split applied buffer plus unconstrained accessors, slider-driven slot timelines target applied pose during world transforms, Rust-only name-based attachment plus bone-reassignment setters have been removed, blend mode is read from `SlotData` instead of copied into runtime slots, and attachment deform reset logic now follows C++ timeline-attachment identity across public and animation paths; `SkinData` now has official-style attachment set/remove/list helpers and growable set semantics; `Skeleton::set_attachment` now follows C++ void/no-op semantics; IK, transform, physics, and slider runtime constraint types and solver helpers have moved into `skeleton::ik`, `skeleton::transform`, `skeleton::physics`, and `skeleton::slider`; generic attachment world-vertices computation has moved into `skeleton::vertices`, and the public slot helper is now documented as `slot_attachment_world_vertices`; `Skeleton` container/state fields have been narrowed behind accessors and official-style color, position, and scale setters; `Bone` local pose, applied pose, active state, and world-transform fields have been narrowed behind accessors and setters; `Slot` pose fields have been narrowed behind accessors and setters; runtime IK, transform, path, physics, and slider constraint pose surfaces have been narrowed behind accessors and setters; official C++ physics translate/rotate controls have been added at both `Skeleton` and `PhysicsConstraint` levels; Skeleton wind/gravity/time/update and skin switching now follow direct C++ setter/no-op semantics; `Skeleton::set_skin` no longer returns a `Result`; `Skeleton::skin()` now returns current `SkinData`; `Skeleton::constraints()` exposes ordered typed constraint refs; named root/bone/slot lookup plus attachment convenience helpers have been added; a no-clipper `Skeleton::bounds` helper now computes active region/mesh AABBs in draw order; `Skeleton::bounds_with_clipping` now mirrors the optional clipper-aware bounds overload; explicit by-name constraint lookup helpers now exist for IK, transform, path, physics, and slider constraints; official-style setup pose split APIs now exist and empty setup attachments reset sequence state like `spine-cpp`; the old `set_to_setup_pose` compatibility alias and dead `Error::UnknownSkin` path have been removed.

# Next Action

Continue `AnimationState` parity audit around C++ `computeHold`/timeline mode representation, remaining `TrackEntry` surface, and dead compatibility code before attempting any `interruptAlpha`-shape refactor. Keep the same verification shape: focused tests first, then the full core parity gate.

# Citations

- [Hardening plan](../../../plans/2026-06-23-001-refactor-spine-cpp-parity-hardening-plan.md)
- [Current state](../current-state.md)
- [Update log](../log.md)
