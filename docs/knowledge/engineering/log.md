# Engineering Memory Update Log

## 2026-06-27
* **Queued mix-duration helper removed**: Deleted the Rust-only `TrackEntryHandle::set_mix_duration_with_delay` surface. `SpineTrackEntrySettings::apply` now resolves the combined mix-duration + delay case inline, and the runtime regression now checks queued negative mix duration directly with `set_mix_duration`. Verification passed: `cargo fmt --all -- --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`659 passed, 2 skipped`), `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`), and `git diff --check`.
* **Rust-only listener clear helpers removed**: `AnimationState::clear_listener` and `TrackEntryHandle::clear_listener` were deleted. Tests now model the same no-op outcome by swapping in an explicit empty listener instead of clearing a stored listener, which trims one more convenience-only Rust API without changing listener replacement behavior. Verification passed: `cargo fmt --all -- --check`, focused nextest for the two replacement tests (`2 passed, 660 skipped`), `cargo test -p spine2d --features json,binary,upstream-smoke --all-features --no-run`, full `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`660 passed, 2 skipped`), and `git diff --check`.
* **Timeline classification moved onto Animation**: `Animation` now owns the additive/instant classification helpers used by `AnimationState`, so runtime hold-mix logic no longer needs its own `timeline_kind_additive` / `timeline_kind_instant` functions. Verification passed: `cargo fmt --all -- --check`, `cargo test -p spine2d --all-features --no-run`, and `cargo nextest run -p spine2d --all-features` (`660 passed, 2 skipped`).
* **AnimationState property ID resolution delegated to the model**: `AnimationState` now calls `Animation::timeline_property_ids` and `Animation::has_timeline_property_ids` directly, and the draw-order folder mix check uses a dedicated model helper for the draw-order property id. Verification passed: `cargo fmt --all -- --check`, `cargo test -p spine2d --all-features --no-run`, and `cargo nextest run -p spine2d --all-features` (`660 passed, 2 skipped`).
* **Attachment ID lookup folded into AttachmentData**: `AttachmentData` now owns the vertex-attachment and sequence-id helpers, so `DeformTimeline` and `SequenceTimeline` can resolve property IDs without a standalone attachment-ID free function. Verification passed: `cargo fmt --all -- --check`, `cargo test -p spine2d --all-features --no-run`, and `cargo nextest run -p spine2d --all-features` (`660 passed, 2 skipped`).
* **Timeline property ID helpers moved toward the model layer**: `DeformTimeline` and `SequenceTimeline` now own the attachment-resolution helpers used by `animation_state.rs` for vertex and sequence property IDs, while deform/sequence slot matching continues to share the attachment helper added in the previous slice. Verification passed: `cargo fmt --all -- --check`, `cargo test -p spine2d --all-features --no-run`, and `cargo nextest run -p spine2d --all-features` (`660 passed, 2 skipped`).
* **Attachment timeline matching helper deduplicated**: `AttachmentData::get_timeline_skin()` now exposes the mesh-only linked-mesh timeline source skin, and deform/sequence slot matching in `animation.rs` routes through a shared attachment-timeline helper instead of carrying two near-identical match blocks. Verification passed: `cargo fmt --all -- --check`, `cargo test -p spine2d --all-features --no-run`, and `cargo nextest run -p spine2d --all-features` (`660 passed, 2 skipped, 1 leaky`).
* **Attachment timeline metadata aligned to C++ base semantics**: Region, point, path, bounding-box, clipping, and mesh attachment data now all carry explicit timeline attachment keys and timeline slot lists in the Rust model. JSON and Binary constructors populate those fields, attachment timeline slot lookup now uses the shared getter path, and the latest-tag C++ attachment timeline behavior is covered by the full crate test run. Verification passed: `cargo fmt --all -- --check`, `cargo test -p spine2d --all-features --no-run`, and `cargo nextest run -p spine2d --all-features` (`660 passed, 2 skipped`).

## 2026-06-26
* **Runtime constraint typed lists narrowed to C++ surface**: IK, transform, path, physics, and slider runtime constraints now expose `get_data`, while `Skeleton` keeps the unified ordered `get_constraints()` view and the official `get_physics_constraints()` list. Rust-only public typed runtime list getters for IK/transform/path/slider were removed; mutation now uses `find_constraint_mut::<T>(name)` or ordered `get_constraints_mut()`. Verification passed: `cargo fmt --all`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, focused mutable-view nextest (`1 passed, 660 skipped`), and clean-target core nextest (`659 passed, 2 skipped`).
* **SkeletonData typed constraint slices removed**: the public `get_ik_constraints` / `get_transform_constraints` / `get_path_constraints` / `get_physics_constraints` / `get_slider_constraints` data getters were deleted. Diagnostic code now uses `SkeletonData::get_constraints()` with indexed enum variants; the subsequent runtime slice applies the same narrowing to the Rust-only IK/transform/path/slider runtime list getters. Verification passed: `cargo fmt --all`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and clean-target `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`658 passed, 2 skipped`).
* **Slider binding reads unified through data lookups**: `SliderConstraintData::get_animation` now resolves through `SkeletonData` with index-first lookup and name fallback, `SliderConstraintData::set_animation` clears the stale index when used as a name-driven setter, and JSON/Binary loaders now populate both the index and animation name so loader and manual APIs stay consistent. Runtime slider initialization and apply now both consume the shared getter path, and the regression test covers a name-only binding plus draw-order output to prove the runtime still reacts correctly after the lookup split. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and clean-target `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`658 passed, 2 skipped`).
* **Clipping end-slot getter now resolves through `SkeletonData`**: `ClippingAttachmentData::get_end_slot` no longer exposes the internal slot index. It now resolves the stored optional index through `SkeletonData` and returns `Option<&SlotData>`, matching the same ownership-safe pattern used for slider animation bindings. Verification passed: `cargo fmt --all -- --check` and clean-target `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`657 passed, 2 skipped`).
* **Slider animation getter now resolves through `SkeletonData`**: `SliderConstraintData::get_animation` no longer returns the internal animation index. It now resolves the stored optional index through `SkeletonData` and returns `Option<&Animation>`, while parser/runtime code continues to store the binding as an internal index. Verification passed: `cargo fmt --all -- --check` and clean-target `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`657 passed, 2 skipped`).
* **SliderData semantic split**: SliderConstraintData now stores C++ property offset, slider offset, and max separately; JSON/Binary parsing and runtime slider application were updated to match latest-tag spine-cpp. Verification passed: cargo fmt --all -- --check and clean-target spine2d nextest with json,binary,upstream-smoke (657 passed, 2 skipped).
* **Bone/slot relation helpers exposed**: `BoneData` now has `get_parent(&SkeletonData)` plus `get_color_mut()`, and `SlotData` now has `get_bone(&SkeletonData)`, so callers can traverse the data ownership graph without reading raw indices directly. The public storage layout stays flattened for now, but the lookup surface is closer to latest-tag `spine-cpp`. Verification passed: focused `model_lookup_tests` (`9 passed, 649 skipped`), full core nextest (`656 passed, 2 skipped`), `cargo fmt --all -- --check`, and `git diff --check`.
* **Attachment data accessor surface expanded**: Commit `72fba7e` added official-style getters/setters for region, mesh, point, path, bounding-box, and clipping attachment data after their fields were narrowed. The surface now covers attachment names plus the editable state visible in latest-tag `spine-cpp`; Rust keeps sequence, vertex, and linked-mesh internals owned and crate-private. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`656 passed, 2 skipped`), `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, and `git diff --check`.
* **Internal model helper types narrowed**: Commit `776c7c1` moved `SequenceDef`, `VertexWeight`, `MeshVertices`, and `TimelineKind` behind crate-private visibility. These helpers still power attachment parsing and timeline ordering internally, but they no longer leak as public API surface. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`656 passed, 2 skipped`), `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, and `git diff --check`.
* **Attachment data fields narrowed**: Commit `eeefb13` moved attachment DTO storage in `spine2d/src/model.rs` behind crate-private fields and added official-style color accessors for the region, mesh, point, path, bounding-box, and clipping attachment data structs. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`656 passed, 2 skipped`), `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, and `git diff --check`.
* **Event model getters aligned**: Commit `3931331` moved `EventData`, `Event`, and `EventTimeline` off broad public fields and onto latest-tag C++-style accessors. `EventData` now exposes `get_name`, `get_setup_pose`, `get_audio_path`, and `set_audio_path`; setup payload defaults live in `EventSetupPose`; `Event` exposes `get_data`, `get_time`, and payload getters/setters; `EventTimeline` exposes `get_events`, `get_frame_count`, and `set_frame`. Rust uses `Arc<EventData>` for events instead of copying C++'s self-referential setup-pose object layout, keeping ownership sound while preserving parser/runtime behavior. JSON audio events now also have focused coverage for the C++ default `volume=1` / `balance=0` when an audio path exists and the key omits overrides. Verification passed via clean `CARGO_TARGET_DIR=/tmp/spine-rs-target-test`: full core nextest (`654 passed, 2 skipped`), focused nextest (`json_event_tests binary_event_timeline model_lookup_tests json_timeline_order_tests upstream_event_timeline_tests`: 15 passed, 641 skipped), selected animation-state event tests (`6 passed, 650 skipped`), core check, core examples, Bevy examples, WGPU/Web checks, `cargo fmt --all -- --check`, and `git diff --check`. WGPU/Web still emits the existing `block v0.1.6` future-incompat warning.
* **AnimationState getters aligned**: Removed Rust-style `AnimationState::data()` / `data_mut()` / `tracks()` / `current()` / `time_scale()` / `manual_track_entry_disposal()` in favor of `get_data()` / `get_data_mut()` / `get_tracks()` / `get_current()` / `get_time_scale()` / `get_manual_track_entry_disposal()`, matching latest-tag C++ getter naming. `TrackEntry::time_scale()` was also renamed to `get_time_scale()`. Verification passed: focused core nextest (`animation_state_tests render_oracle_parity_tests upstream_spine_c_smoke_tests upstream_spine_skel_smoke_tests`: 92 passed, 563 skipped), Bevy focused nextest (`animation`: 15 passed, 28 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Skeleton scalar getters aligned**: Removed the Rust-style `Skeleton::x()` / `y()` / `scale_x()` / `scale_y()` / `wind_x()` / `wind_y()` / `gravity_x()` / `gravity_y()` helpers and replaced them with C++-style `get_x()` / `get_y()` / `get_scale_x()` / `get_scale_y()` / `get_wind_x()` / `get_wind_y()` / `get_gravity_x()` / `get_gravity_y()`. Verification passed: focused nextest (`skeleton_tests oracle_scenario_parity_tests`: 341 passed, 314 skipped), Bevy focused nextest (`skeleton`: 5 passed, 38 skipped), clean-target checks for core, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Skeleton collection getters aligned**: Removed the Rust-style `Skeleton::data()` / `bones()` / `slots()` / `draw_order()` collection helpers and typed constraint list helpers, replacing them with `get_data()` / `get_bones()` / `get_slots()` / `get_draw_order()` plus matching `get_*_mut` and `get_*_constraints()` names. IK, Transform, and Path constraint `bones()` / `bones_mut()` helpers were also renamed to `get_bones()` / `get_bones_mut()`. Verification passed: focused nextest (`skeleton_tests ik_tests slots_tests oracle_scenario_parity_tests`: 349 passed, 306 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Animation timeline getters aligned**: Removed the Rust-style `Animation::bones()` / `timelines()` and `BoneTimeline::bone_index()` query helpers, replacing them with `Animation::get_bones()` / `get_timelines()` and `BoneTimeline::get_bone_index()` to match latest-tag C++ getter naming. Verification passed: focused nextest (`model_lookup_tests json_timeline_order_tests slider_timeline_tests skeleton_tests`: 42 passed, 613 skipped), `cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all -- --check`, and `git diff --check`.
* **AttachmentData name helper aligned**: Removed the Rust-style `AttachmentData::name()` helper and replaced it with `AttachmentData::get_name()`, matching the latest-tag C++ `Attachment::getName()` surface. Updated linked-mesh source lookup, pose dumps, oracle dumps, and attachment-focused tests to use the new getter. Verification passed: focused nextest (`skeleton_tests skin_active_semantics_tests oracle_scenario_parity_tests`: 349 passed, 306 skipped), `cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all -- --check`, and `git diff --check`.
* **ConstraintDataRef getters aligned**: Removed the Rust-style `ConstraintDataRef::name()` and `skin_required()` helpers and replaced them with C++-style `get_name()` / `get_skin_required()` on the unified constraint data view. Added `get_order()` and reused it for `SkeletonData::get_constraints()` ordering plus focused model lookup assertions. Verification passed: focused nextest (`model_lookup_tests`: 7 passed, 648 skipped), `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all -- --check`, and `git diff --check`.
* **Event audio defaults aligned with C++**: Rechecked latest-tag `spine-cpp` `SkeletonJson` / `SkeletonBinary` event loading and fixed Rust event defaults. JSON event keys now inherit `volume` / `balance` from `EventData` setup values only when an audio path exists; no-audio JSON events ignore volume/balance fields and keep C++ `Event` constructor defaults (`0/0`). Binary no-audio event data and event timeline keys now keep `0/0` too. Verification passed: focused nextest (`json_event_tests binary_event_timeline`: 3 passed, 652 skipped), `cargo check -p spine2d --features json,binary,upstream-smoke`, full core nextest (`653 passed, 2 skipped`), `cargo fmt --all -- --check`, and `git diff --check`.
* **SkeletonData version getter renamed to C++ shape**: Removed the Rust-only `SkeletonData::get_spine_version` spelling and exposed `SkeletonData::get_version`, matching latest-tag C++ `SkeletonData::getVersion()`. A focused model test now covers the header metadata getter names. Verification passed: focused nextest (`skeleton_data_header_getters_match_cpp_names model_lookup_tests`: 7 passed, 648 skipped), `cargo fmt --all -- --check`, and `git diff --check`.
* **SkeletonData skin/event arrays aligned with C++**: `SkeletonData.skins` and `SkeletonData.events` now use ordered `Vec` storage, and `get_skins()` / `get_events()` expose slices instead of `IndexMap` values keyed by name. `find_skin` / `find_event` scan object names like `spine-cpp`, while JSON/Binary parsing keeps temporary maps only inside the loader for linked mesh and event timeline resolution. Bevy/Web/WGPU callers now enumerate names from `SkinData::get_name()` / `EventData::name`. Verification passed: focused nextest (`model_lookup_tests json_event_tests skin_active_semantics_tests skeleton_tests`: 45 passed, 609 skipped), full core nextest (`652 passed, 2 skipped`), Bevy nextest (`43 passed`), clean-target checks for core, core examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Full clean-target parity gate passed**: After the MeshAttachment metadata slice, the full core runtime and integration checks passed via clean `CARGO_TARGET_DIR=/tmp/spine-rs-target-test`: core nextest (`652 passed, 2 skipped`), Bevy nextest (`43 passed`), core feature check, core examples check, Bevy examples check, and WGPU/Web check. `cargo fmt --all -- --check` and `git diff --check` also passed. The default repo `target/` reproduced the known stuck `rustc`/`cargo check` behavior during integration-package checks, so those self-started stale processes were terminated and the clean target remains the reliable gate path for now.
* **MeshAttachment public data surface aligned**: `MeshAttachmentData` now preserves latest-tag C++ `MeshAttachment` inspection fields: `hull_length`, `edges`, `width`, and `height`. JSON and binary loaders store the values they previously ignored, with hull length stored in the C++ `getHullLength()` shape (`hull << 1`), and linked meshes inherit these fields from their source mesh like C++ `setSourceMesh`. Verification passed: `cargo fmt --all -- --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, and focused nextest via clean `CARGO_TARGET_DIR=/tmp/spine-rs-target-test` (`deform_tests skeleton_tests`: 36 passed, 618 skipped; `binary_tests`: 10 passed, 644 skipped).
* **Skin slot query helpers restored with C++ shape**: Added `SkinData::find_names_for_slot(slot, &mut Vec<String>)` and `SkinData::find_attachments_for_slot(slot, &mut Vec<&AttachmentData>)`, matching latest-tag C++ `Skin::findNamesForSlot` / `findAttachmentsForSlot` append semantics without restoring the deleted Rust-only helper names. Verification passed: `cargo fmt --all -- --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, and focused tests via `CARGO_TARGET_DIR=/tmp/spine-rs-target-test` (`cargo test ... skin_find_slot_queries_append_like_cpp` 1 passed; nextest `skin_active_semantics_tests` 8 passed, 646 skipped). The default repo `target/` had a stale test binary launch/listing issue, so the focused test gate used a clean temporary target directory.
* **AnimationState listener clearing aligned**: Added `AnimationState::clear_listener` and `TrackEntryHandle::clear_listener` as the safe Rust equivalents of resetting latest-tag C++ listeners back to the dummy/no-op callback. Focused regressions cover state-level and entry-level listener clearing. Verification passed: `cargo fmt --all -- --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused listener clear nextest (`2 passed` across two commands), focused `animation_state_tests` (`81 passed, 572 skipped`), and `cargo check -p spine2d-bevy --examples`.
* **Empty-track addAnimation callback order aligned**: `AnimationState::add_animation` and `add_empty_animation` now follow latest-tag C++ by draining the Start event for an empty track before writing caller-provided delay, empty mix duration, and empty track end onto the entry. Start listeners now observe constructor defaults, and `TrackEntry::track_end` defaults to `f32::MAX` like C++ `FLT_MAX` rather than infinity. Verification passed: `cargo fmt --all -- --check`, `git diff --check`, core check, and focused `animation_state_tests` (`79 passed, 572 skipped`).
* **AnimationState drain flag aligned with C++ EventQueue**: Removed the separate Rust-only `draining_events` state and now uses `drain_disabled` as the single reentrant-drain guard, matching latest-tag `spine-cpp` `EventQueue::_drainDisabled`. A new regression covers `disable_queue()` called inside a listener: it suppresses reentrant drains only for the current drain and is cleared afterward like C++. Verification passed: `cargo fmt --all -- --check`, `git diff --check`, core check, focused `animation_state_tests` (`77 passed, 572 skipped`), focused `animation_state_mixing_semantics_tests` (`16 passed, 633 skipped`), Bevy examples check, and Bevy nextest (`43 passed, 0 skipped`).
* **AnimationState listener snapshot layer deleted**: Removed the Rust-only `TrackEntrySnapshot` type and now dispatches listener callbacks with live `TrackEntryHandle` values, closer to latest-tag `spine-cpp` passing `TrackEntry*` through `EventQueue::drain`. Also deleted `TrackEntryHandle::with_entry`; repo callers now use `entry(&state)` while keeping the official-style `animation_state`, `is_next_ready`, `previous` / `next`, and mixing links directly. Verification passed: `cargo fmt --all -- --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `animation_state_tests` / `animation_state_mixing_semantics_tests` (`92 passed, 556 skipped`), `cargo check -p spine2d-bevy --examples`, and Bevy nextest (`43 passed, 0 skipped`).

## 2026-06-25
* **Skeleton/Bone tuple helper surface trimmed**: Deleted the Rust-only tuple-returning `Skeleton::position`, `Skeleton::scale`, `Skeleton::wind`, `Skeleton::gravity`, `Bone::position`, `Bone::scale`, `Bone::applied_position`, `Bone::applied_scale`, `Bone::applied_shear`, and `Bone::world_position` helpers, then rewired the Bevy runtime-state sync plus skeleton tests/examples to use component getters directly. I also removed the Rust-only tuple setters `Skeleton::set_wind`, `Skeleton::set_gravity`, `Bone::set_applied_position`, `Bone::set_applied_scale`, `Bone::set_applied_shear`, and `Bone::set_world_position`; callers now use the component setters that mirror the C++ `BoneLocal` / `BonePose` surface. `PointAttachmentData::compute_world_position` / `compute_world_rotation` were explicitly kept after rechecking C++ `PointAttachment`. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke skeleton_tests point_attachment_tests --no-fail-fast --status-level fail` (`31 passed, 617 skipped`), `cargo check -p spine2d-bevy --examples`, and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`).
* **Slot bone accessor aligned with C++**: Added `Slot::get_bone(&Skeleton)` to mirror C++ `Slot::getBone()`, deleted the public `Skeleton::slot_attachment_data` wrapper, and migrated the remaining pose/example/oracle attachment lookups to the slot-level helpers. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, and focused `runtime::slots_tests` / `runtime::skeleton_tests` / `runtime::skin_active_semantics_tests` / `runtime::oracle_scenario_parity_tests` / `runtime::animation_state_mixing_semantics_tests` (`366 passed, 282 skipped`).
* **Slot dark-color helper naming aligned with C++**: Renamed `has_dark` / `set_has_dark` / `applied_has_dark` to `has_dark_color` / `set_has_dark_color` / `applied_has_dark_color`, then updated the animation/render/example/test call sites. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, and focused `runtime::slots_tests` / `runtime::skeleton_tests` / `runtime::skin_active_semantics_tests` / `runtime::oracle_scenario_parity_tests` / `runtime::color_timeline_tests` / `runtime::examples_pose_parity_tests` / `runtime::animation_state_mixing_semantics_tests` (`378 passed, 270 skipped`).
* **Slot attachment switching logic deepened**: Moved the `clear_deform` and `sequence_index = -1` reset behavior into `SlotPoseMut::set_attachment` so the helper itself now mirrors the official C++ `SlotPose::setAttachment` shape more closely. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `git diff --check`, and focused `runtime::slots_tests` / `runtime::skeleton_tests` / `runtime::skin_active_semantics_tests` / `runtime::oracle_scenario_parity_tests` / `runtime::color_timeline_tests` / `runtime::examples_pose_parity_tests` / `runtime::animation_state_mixing_semantics_tests` (`378 passed, 270 skipped`).
* **Model layer reshaped toward C++**: `BoneData`, `SlotData`, `SkinData`, `ConstraintDataRef`, and `SkeletonData` were tightened toward the latest-tag C++ public model. Explicit indices moved onto the data objects, `SlotData` now keeps setup pose in a nested structure, `SkinData::new` dropped the slot-count seed and now exposes C++-style getters plus `get_attachment`, `ConstraintDataRef` became typed-ref only, `SkeletonData::get_constraints` exposes the unified ordered view, and the old `animation_index` cache was deleted in favor of searching the final animation list. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `git diff --check`, and focused `runtime::slots_tests` / `runtime::skeleton_tests` / `runtime::skin_active_semantics_tests` / `runtime::oracle_scenario_parity_tests` / `runtime::color_timeline_tests` / `runtime::examples_pose_parity_tests` / `runtime::animation_state_mixing_semantics_tests` (`378 passed, 270 skipped`).
* **Runtime wrapper helpers pruned**: Deleted the Rust-only `data_index` / `bone_index` helper methods from the IK, Path, Physics, Slider, and Transform runtime wrappers, promoted `Bone::get_parent` / `get_children` to the C++ hierarchy-accessor spelling, and migrated Bevy / web / wgpu call sites to the new getter-based model surface. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, focused `cargo nextest run -p spine2d --features json,binary,upstream-smoke model_lookup_tests binary_tests json_physics_defaults_tests --no-fail-fast --status-level fail` (`21 passed, 627 skipped`), and focused `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`).
* **Skeleton naming tightened toward C++ getters**: Renamed `Skeleton::root_bone` to `get_root_bone`, `Bone::parent` to `get_parent`, `Bone::children` to `get_children`, and `Skeleton::time` to `get_time`. The rest of the runtime and Bevy/test call sites were updated to the new names. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, and focused `runtime::skeleton_tests` / `runtime::skin_active_semantics_tests` / `runtime::animation_state_mixing_semantics_tests` (`52 passed, 596 skipped`).
* **BoneData and SlotData relationship getters added**: `BoneData` and `SlotData` now expose C++-style `getIndex()` plus relationship getters (`BoneData::getParent()` and `SlotData::getBoneData()` in Rust form), making the setup-data object graph easier to traverse without relying only on raw indices. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `model_lookup_tests` / `json_physics_defaults_tests` / `binary_tests` (`21 passed, 627 skipped`), and backend/example checks (`cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`).
* **Bone and slot setup data indexes aligned**: `BoneData` and `SlotData` now carry explicit indices with C++-style getters so the public setup-data surface matches the latest-tag object model more closely. The fields are populated by JSON and binary parsing, and the existing hand-built fixtures were updated to include indices where they construct these data objects directly. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `model_lookup_tests` / `json_physics_defaults_tests` / `binary_tests` (`21 passed, 627 skipped`), and backend/example checks (`cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`).
* **BoneData getter surface hardened and SlotData surface clarified**: `BoneData` now exposes C++-style getters/setters for name, parent index, length, color, icon, icon size, icon rotation, visible, and skin-required state; `SlotData` now exposes name, bone index, attachment name, color, dark color, blend mode, and visible state while staying aligned with the current Rust model shape after rechecking the C++ inheritance layer. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `model_lookup_tests` / `json_physics_defaults_tests` / `binary_tests` (`21 passed, 627 skipped`), and backend/example checks (`cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`).
* **Skeleton and data getter naming tightened**: Renamed `Skeleton::skin` to `get_skin`, `Skeleton::color` to `get_color`, `SkeletonData::constraints` to `get_constraints`, and `SkeletonData::default_skin` to `get_default_skin` so the Rust surface reads more like the official C++ getter naming. I explicitly left `Slot` value-object accessors alone after one broad replacement attempt proved they are a separate layer with their own stable shape. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `model_lookup_tests` / `runtime::skeleton_tests` / `runtime::slots_tests` (`36 passed, 611 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **SkeletonData read APIs moved toward getter naming**: Renamed `SkeletonData::constraints` to `get_constraints`, `SkeletonData::default_skin` to `get_default_skin`, and kept the existing typed find helpers. This is a small surface cleanup that makes the Rust data model read more like the official C++ getter naming without changing storage or behavior. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `model_lookup_tests` / `runtime::skeleton_tests` / `runtime::slots_tests` (`36 passed, 611 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **Skin attachment lookup and constraint view simplified**: Renamed `SkinData::attachment` to `get_attachment` across the repo so the Rust model matches the C++ getter spelling, then removed the redundant stored index from `ConstraintDataRef` and kept only typed references. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `model_lookup_tests` (`5 passed, 642 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **ConstraintDataRef index helper deleted**: Removed the Rust-only `ConstraintDataRef::data_index()` helper after rechecking the enum shape; the index is already carried in the enum variant, so callers can pattern match directly. Verification passed: `cargo fmt --all -- --check`, focused `model_lookup_tests` (`5 passed, 642 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **Attachment world-vertex bridge moved out of `Skeleton`**: `slot_attachment_world_vertices` was deleted from `Skeleton`, and vertex-bearing attachment math now lives on `AttachmentData::compute_world_vertices`. Call sites in the pose oracle and example dump now resolve the current attachment first and then compute world vertices from the attachment value object. Verification passed: `cargo fmt --all -- --check`, focused runtime parity nextest (`348 passed, 299 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **Runtime object index getter surface trimmed**: Deleted the Rust-only public `data_index` / `bone_index` / `parent_index` / `set_bone_index` accessors from runtime pose and constraint objects, then rewired internal call sites to read the crate-visible fields directly. This keeps the public runtime surface closer to latest-tag `spine-cpp` while still preserving the indexed storage model used throughout the crate, render path, and oracle tests. Verification passed: `cargo fmt --all` and both focused runtime parity nextest plus the full core gate (`51 passed, 596 skipped`; `645 passed, 2 skipped`).
* **SkinData constructor narrowed**: Changed `SkinData::new` to accept only the skin name and initialize an empty attachment bucket list. Call sites that need fixed slot capacity now resize `attachments` explicitly in tests or setup helpers, which removes another Rust-only convenience parameter from the public model while keeping the underlying storage and behavior intact. Verification passed: `cargo fmt --all -- --check`, focused `runtime::skin_active_semantics_tests` / `model_lookup_tests` (`12 passed, 635 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **SkinData enumeration helpers removed**: Deleted the Rust-only `SkinData::names_for_slot`, `attachments_for_slot`, and `attachment_entries` helpers. Tests now assert directly on the underlying ordered `attachments` buckets, which keeps the public model closer to the official C++ skin surface and removes a layer of convenience-only inspection API. Verification passed: `cargo fmt --all -- --check`, focused `runtime::skin_active_semantics_tests` (`7 passed, 640 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **Parity progress assessment updated**: The latest comparison against pinned latest-tag `spine-cpp` suggests the runtime is no longer carrying a large behavior gap. The remaining divergence is mostly public Rust convenience surface: `SkinData` still has `names_for_slot`, `attachments_for_slot`, and `attachment_entries`, while core runtime behavior around `Skeleton`, `AnimationState`, and parser order is already much closer to the official C++ shape. The next cleanup slice should likely delete or collapse those inspection helpers and then re-run the parity gate.
* **Public SkeletonData animation index removed**: Deleted the public `SkeletonData.animation_index` field and kept animation lookup tables parser-local inside JSON and binary loaders. Callers that need an index now resolve it from `data.animations` by name, which keeps the public data model closer to latest-tag `spine-cpp` and removes the stale Rust-only synchronization surface. Verification passed: `cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke slider_timeline_tests --no-fail-fast --status-level fail`, and full core nextest (`645 passed, 2 skipped`).
* **JSON animation lookup table removed**: Deleted the last Rust-only animation lookup table from the JSON loader. Slider-constraint animation references now resolve against the final `animations` array by name during parsing, which matches the C++ `findAnimation` lookup shape while avoiding a redundant synchronization map. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused slider tests (`3 passed, 644 skipped`), and full core nextest (`645 passed, 2 skipped`).
* **Mutable Skeleton convenience helpers trimmed further**: Removed Rust-only `root_bone_mut`, the `find_*_mut` name lookup helpers, `attachment_by_slot_name`, and `skin_name`. `set_skin(Some(current_skin))` and `set_skin(None)` now no-op when state would not change, which better matches the C++ setter behavior. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused `skeleton_tests` / `skin_active_semantics_tests` (`36 passed, 611 skipped`), and full core nextest (`645 passed, 2 skipped`).

## 2026-06-18
* **Initialization**: Created engineering wiki memory bundle.
* **Historical 4.3.4 baseline snapshot**: The active parity baseline for this early batch was `spine-flutter-4.3.4` (`80dc680a4345ac09cdc5d4c1a77ec572a3f295d1`), not `4.3.2`. This is superseded by the current pinned latest tag `spine-ts-4.3.8` (`8e12b1250ab88c0f890849ea45aab80338cead63`).
* **Run-to-walk `.skel` failure isolated**: JSON scenario `oracle_spineboy_run_to_walk_mix0_2_t0_4_matches_cpp` passes. `.skel` scenario at `t=0.4` still fails on `rear-foot applied.rotation` by about `0.0115` degrees. Debug dumps show the mismatch emerges in rear-leg 2-bone IK after near-identical pre-world inputs, not from a simple `rear-foot-ik bendDirection` parse error.

## 2026-06-19
* **Run-to-walk `.skel` parity restored**: Refreshed the stale `spineboy_run_to_walk_mix0_2_t0_4` `.skel` golden to the then-current `spine-flutter-4.3.4` output. `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` now passes with `457 passed, 10 skipped`.
* **Draw batching parity locked**: Added direct `render::append_indexed` unit tests covering batch merge, color/dark-color split, and the 16-bit index limit. This closes the remaining draw batching checklist item against the official `SkeletonRenderer` batching rules.
* **WASM compile smoke passed**: `cargo check -p spine2d --target wasm32-unknown-unknown --features json,binary` passed, confirming the core crate still builds for the target the roadmap calls out as first-class.

## 2026-06-20
* **Latest upstream parity suite green**: `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` passed with `533 passed, 10 skipped`.
* **Golden baseline refreshed**: Re-recorded stale `sack_*` physics oracle goldens against the then-current `spine-flutter-4.3.4` commit `80dc680a4345ac09cdc5d4c1a77ec572a3f295d1`.
* **Formatting gate green**: `cargo fmt --all --check` passed.

## 2026-06-23
* **AnimationState internal clock field deleted**: Removed the leftover Rust-only `time: Cell<f32>` state after the public `AnimationState::time()` getter had already been deleted. `spine-cpp` has no matching accumulated clock, and the field was only incremented inside `update` with no readers. Verification passed on 2026-06-24: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and full core nextest (`623 passed, 10 skipped`).
* **Same-unapplied replacement coverage added**: Added a focused `AnimationState` regression for C++ `current->_nextTrackLast == -1 && current->_animation == &animation`: setting the same animation again before first apply replaces the old entry without mixing, disposes the old entry, and emits interrupt/end/dispose/start. Verification passed on 2026-06-24: focused nextest, full `runtime::animation_state_tests` (`76 passed, 557 skipped`), and full core nextest (`623 passed, 10 skipped`).
* **AnimationState End/Dispose queue boundary aligned**: Rust now mirrors C++ `EventQueue::drain` by making queued `End` entries fall through to `Dispose` notification and automatic release at drain time. Normal completion, clear-track, same-unapplied replacement, and mixing-from completion paths now queue only `End`; queued entries removed by `clearNext`-style paths still queue direct `Dispose`. Verification passed on 2026-06-24: `runtime::animation_state_tests` (`73 passed, 556 skipped`), `runtime::animation_state_mixing_semantics_tests` (`16 passed, 613 skipped`), focused lifecycle regressions (`3 passed, 626 skipped`), `cargo fmt --all --check`, `git diff --check`, wiki validation, and full core parity (`619 passed, 10 skipped`, `1 leaky`).
* **Two-bone IK boundary aligned for binary `.skel` parity**: Rechecked `spineboy_run_to_walk_mix0_2_t0_4` against the direct `spine-cpp` oracle and confirmed the existing `.skel` golden was correct. Rust diverged because last-bit matrix/IK input drift pushed the two-bone IK cosine one `f32` ULP above `1.0`, selecting the hard-stretch branch while C++ stayed on the tiny-bend `acos` path. `skeleton::ik` now treats only that one-ULP overrun as in-domain, and BonePose angle/matrix math was nudged toward the C++ numeric shape. Verification passed on 2026-06-24: focused `.skel` spineboy oracle regressions, focused IK/skeleton tests, focused additive spineboy scenarios, `cargo fmt --all --check`, `git diff --check`, and full core parity (`619 passed, 10 skipped`).
* **Draw-order and attachment helper shells trimmed further**: Removed the last single-use `Skeleton::slot_pose_ref` helper, deleted the thin `set_draw_order_to_setup` / `set_draw_order_from_slice` wrappers, and let `animation.rs` write draw-order targets directly. The runtime behavior stayed the same; the helper surface is just flatter and easier to audit. Verification passed on 2026-06-24: `cargo fmt --all --check`, full core parity (`619 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **Draw-order dispatch state simplified**: Removed the redundant `draw_order_folder_out` field from `StateTimelineApply` and collapsed `draw_order_out` from `Option<bool>` to `bool`. `AnimationState` now derives the folder draw-order `out` branch directly from `MixDirection`, which keeps the dispatch surface thinner without changing parity. Verification passed on 2026-06-24: `cargo fmt --all --check`, full core parity (`619 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **AnimationState handle helper shell trimmed again**: Removed the last single-use `entry_for_handle_mut` helper and inlined the remaining `MixFrom::from_blend` mapping at call sites. The observable runtime behavior stayed the same; the helper surface is just smaller and easier to audit now. Verification passed on 2026-06-24: `cargo fmt --all --check`, full core parity (`619 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **AnimationState helper shell trimmed**: Inlined the last single-use `previous_entry_for` helper and flattened the current-entry/current-mixing-from timeline mode mapping inside `AnimationState`, leaving the observable parity surface unchanged but making the module thinner and easier to audit. Verification passed on 2026-06-24: `cargo fmt --all --check`, full core parity (`619 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **Current-entry apply helper signature tightened**: `AnimationState::apply_entry_pose` no longer threads a one-off direction parameter through the current-entry path; the call site now passes the fixed `MixDirection::In` directly, which keeps the refactor closer to the official C++ current-entry branch shape without changing behavior. Verification passed on 2026-06-24: `cargo fmt --all --check`, full core parity (`619 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **Negative track-time sentinels aligned**: `AnimationState::set_animation`, `AnimationState::update_mixing_from`, and `TrackEntry::wasApplied`-style readiness now follow the C++ `-1` unapplied sentinel exactly instead of using `< 0` checks. This fixes the case where a previously-applied entry with negative `track_time` or `next_track_last_time` was misclassified as fresh, which could incorrectly skip mixing-from cleanup or same-animation reuse. Verification passed on 2026-06-24: focused regressions `negative_track_end_clears_when_track_last_passes_it`, `completed_hold_mix_chain_with_negative_track_time_still_detaches`, and `set_animation_same_animation_with_negative_track_time_still_mixes_from_applied_entry`, `cargo fmt --all --check`, full core parity (`617 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **Linear mix ratios now pass through negative values**: `TrackEntry::mix_percent()` no longer clamps the linear interpolation path before returning, so negative mix ratios from negative `mix_time / mix_duration` now propagate like C++ `TrackEntry::mix()`. A focused `runtime::animation_state_mixing_semantics_tests` gate passed with `15 passed, 609 skipped` after the change.
* **Crate-level dead-code masks removed**: Deleted the blanket `#![allow(dead_code)]` from `spine2d/src/geometry.rs` and `spine2d/src/binary_tests.rs`. Both modules still compile cleanly under `cargo fmt --all --check` and `cargo check -p spine2d --features json,binary,upstream-smoke`, so the blanket waivers were no longer carrying weight.
* **Completed mixing-from chain cleanup aligned**: `AnimationState::update` now consumes the `update_mixing_from` completion return like C++ `AnimationState::update`, clearing the current entry's remaining `mixing_from` chain and queuing End/Dispose for leftovers once all mixing-from entries have completed. This fixes hold-mix cases where a nonzero `totalAlpha` previously left `current.mixing_from` attached after the mix duration elapsed. Verification passed on 2026-06-24: focused regression `completed_hold_mix_chain_detaches_current_mixing_from`, `runtime::animation_state_mixing_semantics_tests` (`14 passed, 609 skipped`), `runtime::animation_state_tests` (`69 passed, 554 skipped`), full core parity (`613 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **Attachment state naming clarified**: internal attachment-state constant `ANIMATION_STATE_CURRENT` was renamed to `ANIMATION_STATE_RETAIN`, matching C++ `AttachRetain` terminology. This is a non-behavior cleanup to make future attachment timeline audits less misleading; verification stayed green with the same `613 passed, 10 skipped` core gate and Bevy/backend checks.
* **DrawOrder Current mix-out totalAlpha aligned**: `AnimationState::apply_mixing_from_pose` now mirrors C++ `applyMixingFrom` by skipping non-hold `DrawOrderTimeline` entries before adding to `total_alpha` when `drawOrder == false` and `mixFrom == Current`. This fixes a lifecycle edge where outgoing draw-order-only entries could keep `mixing_from` alive after the mix completed. Verification passed on 2026-06-24: focused regression `draw_order_current_mix_out_does_not_keep_mixing_from_alive`, `runtime::animation_state_mixing_semantics_tests` (`13 passed, 609 skipped`), `runtime::animation_state_tests` (`69 passed, 553 skipped`), full core parity (`612 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry/AnimationState direct-return API aligned**: `AnimationStateData::set_mix`, `AnimationState::set_animation`, and `AnimationState::add_animation` now match latest-tag C++ direct-return/assert semantics. Missing animation names panic instead of returning `Result`, and Bevy missing-animation commands now expect that panic while preserving prior entry settings. Verification passed on 2026-06-24: full core parity (`611 passed, 10 skipped`), `runtime::animation_state_tests` (`69 passed, 552 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy/WGPU/Web checks, Python byte-compile, zsh syntax checks, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry test-only helper hooks removed**: Deleted the runtime-only `queue_front_delay_for_tests` and `track_entry_exists_for_tests` helpers from `AnimationState`, then rewired `animation_state_tests` to use the public `get_current()` / `next()` / `entry(&state)` / `animation_state()` handle API for the same assertions. Verification passed on 2026-06-24: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and focused `runtime::animation_state_tests` nextest (`69 passed, 554 skipped`).
* **TrackEntry queue lifecycle tightened**: queued-entry promotion now clears the old current entry's `next`; empty-track queued starts mark `animations_changed`; negative `set_delay` is ignored again; stale neighbor handles filter disposed targets; manual disposal preserves disposed queued links until explicit disposal.
* **Render oracle made scenario-only**: Removed the legacy single-animation render-oracle path from `scripts/spine_cpp_lite_render_oracle.cpp` and `spine2d/examples/render_dump.rs`, switched `scripts/record_oracle_render_goldens.py` and `scripts/render_parity_smoke.zsh` to scenario commands only, and re-recorded the render goldens. Verification passed on 2026-06-24: `cargo check -p spine2d --example render_dump --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke render_oracle_parity_tests --no-fail-fast --status-level fail` (`4 passed, 625 skipped`), `./scripts/render_parity_smoke.zsh`, `cargo fmt --all --check`, full core parity (`619 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), `git diff --check`, and `python3 scripts/check_spine_baseline.py --verify-local`.
* **One-shot pose dump deleted**: Removed `spine2d/examples/pose_dump.rs` and its Cargo example entry. Pose oracle sampling now goes through `pose_dump_scenario`, which covers the old single-track sample shape plus mixing and multi-track scenarios. Verification passed on 2026-06-24: `cargo check -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `zsh -n scripts/run_spine_cpp_lite_oracle.zsh`, scenario JSON smoke, and a repo search for stale `pose_dump` references.
* **Unused track-index error removed**: Deleted the dead `Error::InvalidTrackIndex` variant left over from older Rust `Result`-returning track APIs. Runtime track operations now follow the current C++-aligned assert/no-op shape; JSON parse validation still keeps `Error::InvalidValue` for invalid exported data. Verification passed on 2026-06-24: core check, format, diff check, and stale-reference search.
* **Post-cleanup parity gate passed**: After the scenario-only oracle cleanup, one-shot pose dump deletion, and dead error variant removal, verification passed on 2026-06-24: full core parity (`619 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core examples, Bevy examples, WGPU/Web checks, format, diff check, baseline check, and wiki validation. WGPU/Web still emits the known `block v0.1.6` future-incompatibility warning.
* **Latest-tag TrackEntry API supersedes stale checkout findings**: Rechecked pinned `spine-ts-4.3.8` (`8e12b1250ab88c0f890849ea45aab80338cead63`) and detached `repo-ref/spine-runtimes` to that tag. Latest C++ exposes `TrackEntry::getAdditive/setAdditive` and `getMixInterpolation/setMixInterpolation`, with no public `mixBlend` or `holdPrevious`. The Rust, Bevy, scenario, and oracle surfaces were realigned to `additive` / `mix_interpolation`; old `mix_blend` / `hold_previous` APIs and `--entry-mix-blend` / `--entry-hold-previous` CLI options were deleted again. Verification passed: focused mixing semantics, render oracle JSON/SKEL scenarios, full core parity (`610 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy/WGPU/Web checks, `cargo fmt --all --check`, Python byte-compile, zsh syntax checks, and C++ pose/render oracle smokes with `--entry-additive 1`.
* **TrackEntry mix interpolation restored**: `TrackEntry` now stores `MixInterpolation` again and uses it when computing mix percentage, matching C++ `TrackEntry::mix()` instead of always staying linear. The Rust runtime now exposes `mix_interpolation` / `set_mix_interpolation` / `with_mix_interpolation` on entry settings and handles, and also restored the C++ `keepHold` hold-chain state bit for hold preservation. Verification passed: focused interpolation/hold tests, full core parity nextest (`610 passed, 10 skipped`), `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all --check`, and `git diff --check`.
* **Remaining mixDuration zero checks aligned**: `AnimationState` now uses exact C++ `mixDuration == 0` / `to_mix_duration == 0` checks in the remaining single-frame and mix-completion branches, so zero mix durations keep the single-frame path while negative mix durations stay preserved on queued entries. Verification passed: focused mix-duration coverage (`queued_negative_mix_duration_is_preserved_on_activation` and `add_animation_settings_adjust_queued_delay_with_mix_duration`), full core parity nextest (`608 passed, 10 skipped`), Bevy examples, WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **Reverse playback event queueing aligned**: Removed the Rust-only reverse EventTimeline collector. C++ sets the event collection pointer to null for reverse current and mixing-from entries, so reverse playback no longer queues EventTimeline events while complete handling still follows `queueEvents`. Verification passed: focused reverse/AnimationState nextest (`67 passed, 550 skipped`), full core parity nextest (`607 passed, 10 skipped`), core check, Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **EventTimeline and queueEvents layering restored**: `collect_events` now mirrors C++ `EventTimeline::apply` as a raw event collector, while `AnimationState::apply_entry_events_and_complete` owns the C++ `queueEvents` ordering/filtering layer. This removed Rust-only animation window filtering from the collector and preserves C++ signed remainder behavior for `trackLastWrapped`, so first-apply event ordering stays aligned with upstream. Verification passed: focused queue/EventTimeline nextest (`9 passed, 608 skipped`), `runtime::animation_state_tests upstream_event_timeline_tests` (`68 passed, 549 skipped`), full core parity nextest (`607 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, `git diff --check`, and wiki validation.
* **Looping complete zero-duration check made exact**: Looping complete/event splitting now uses C++ exact `duration == 0` / `duration != 0` checks instead of treating near-zero durations as zero with `TIME_EPSILON`. Verification passed: focused loop/event nextest (`7 passed, 609 skipped`), `runtime::animation_state_tests` (`66 passed, 550 skipped`), full core parity nextest (`606 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Non-looping complete boundary made exact**: `AnimationState` non-looping complete detection now uses C++ direct comparisons, `animationTime >= animationEnd && animationLast < animationEnd`, without epsilon padding. Verification passed: focused event/complete nextest (`5 passed, 610 skipped`), `runtime::animation_state_tests` (`65 passed, 550 skipped`), full core parity nextest (`605 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Animation time comparisons made exact**: Removed the remaining Rust-only epsilon tolerance from `TrackEntry::animation_time()`. Loop zero-duration and non-looping `animation_end >= animation.duration` checks now use direct C++ comparisons. Verification passed: focused exact-duration nextest (`2 passed, 612 skipped`), `runtime::animation_state_tests` (`64 passed, 550 skipped`), full core parity nextest (`604 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Looping trackComplete aligned with C++ integer cast**: `TrackEntry::track_complete()` now uses truncation like C++ `(int)(trackTime / duration)` instead of Rust `floor()` for looping entries. This matters for negative track time and queued-delay calculations. Verification passed: focused loop-time nextest (`2 passed, 611 skipped`), `runtime::animation_state_tests` (`63 passed, 550 skipped`), full core parity nextest (`603 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Looping animation time aligned with C++ fmod**: Removed Rust-only normalization from looping `TrackEntry::animation_time()`. It now follows C++ `MathUtil::fmod(trackTime, duration) + animationStart`, so exact loop boundaries return `animation_start` and negative track times keep signed-remainder behavior. Verification passed: focused animation-start/end nextest (`5 passed, 607 skipped`), `runtime::animation_state_tests` (`62 passed, 550 skipped`), full core parity nextest (`602 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **AnimationStateData mix accessors made direct-return**: `AnimationStateData::set_mix_ref` now returns `()` and `get_mix` returns `f32`, matching C++ `setMix(Animation&, Animation&)` / `getMix(Animation&, Animation&)` now that the animation-object path no longer validates against `SkeletonData`. Verification passed: focused `animation_state_data` nextest (`8 passed, 603 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Animation object inputs aligned with C++**: Breaking commit `bdaa7db` made `AnimationState::set_animation` / `add_animation` and `TrackEntryHandle::set_animation` preserve supplied `Animation` objects for the C++ `Animation&` path instead of canonicalizing by name. Name inputs still resolve through `SkeletonData`, animation-object inputs carry their own identity for the same-animation replacement branch, and `AnimationStateData` mix storage keys by animation name like C++ `AnimationPair::operator==`. Verification passed: full core parity nextest (`600 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry animation setter added**: Commit `578b3bf` added `TrackEntryHandle::set_animation`, the safe Rust equivalent of C++ `TrackEntry::setAnimation(Animation&)`. The initial slice preserved timing/mix fields and later received the C++ `Animation&` correction above after checking local `repo-ref/spine-runtimes/spine-cpp`.
* **TrackEntrySnapshot animation index removed**: Breaking commit `9021313` deleted `TrackEntrySnapshot::animation_index`. Listener snapshots now expose `animation_name`, matching what C++ listeners can read through `TrackEntry::getAnimation()`, and event tests now assert animation names instead of internal animation indices. Verification passed: focused `runtime::animation_state_tests` nextest (`57 passed, 550 skipped`), full core nextest (`597 passed, 10 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Bevy runtime-state animation index removed**: Commit `2211af7` deleted the public Bevy `SpineTrackState::animation_index` field and the temporary runtime-state index lookup helper. `animation_name` remains as the public identifier, while compile-only validation passed via `cargo clean -p spine2d-bevy && cargo test -p spine2d-bevy --no-run`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check`. Local Bevy runtime test binaries currently hang before harness output, so no runtime Bevy test pass is recorded for this slice.
* **AnimationState internal clock getter deleted**: Breaking commit `d37dcf8` removed Rust-only `AnimationState::time()`, which has no public `spine-cpp` counterpart. Local tests now assert queue progression and negative-delta behavior through `TrackEntry::track_time` instead of reading the state clock. Verification passed: focused `cargo test` filters for `state_time_scale_scales_update_and_queue_progression` and `update_accepts_negative_delta_like_cpp`, focused nextest for `update_accepts_negative_delta_like_cpp` (`1 passed, 606 skipped`), full core nextest (`597 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry index/internal getters deleted**: Commit `00469de` removed Rust-only `TrackEntry::animation_index` and `TrackEntry::total_alpha`. Follow-up runtime lookups now use the C++-style `SkeletonData::find_animation(name)` helper when an index is needed from parser-local context. Verification passed: `cargo fmt --all --check`, `git diff --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`597 passed, 10 skipped`), `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`), `cargo check -p spine2d-bevy --examples`, and `cargo check -p spine2d-wgpu -p spine2d-web`.
* **Autonomous parity hardening started**: Created `docs/plans/2026-06-23-001-refactor-spine-cpp-parity-hardening-plan.md`; full baseline gate `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` passed with `544 passed, 10 skipped`.
* **C++ reference narrowed**: Active baseline moved to latest verified 4.3 tag `spine-ts-4.3.8` (`8e12b1250ab88c0f890849ea45aab80338cead63`) as the reproducibility anchor. Runtime parity now treats `spine-cpp` as the sole behaviour reference; other runtime tags are metadata only.
* **Skeleton legacy code deleted**: Commit `fbc85eb` removed 634 lines of permanently disabled `#[cfg(any())]` Skeleton solver code. Post-cleanup verification stayed green: `cargo fmt --all --check`, `git diff --check`, and `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast` (`544 passed, 10 skipped`).
* **Timeline dispatch centralized**: Commit `73edc54` moved plain/applied animation dispatch and `AnimationState` current/mixing-from timeline application onto shared internal helpers in `spine2d/src/runtime/animation.rs`. Focused nextest (`animation_state animation`) passed with `76 passed, 478 skipped`; full parity gate stayed green with `544 passed, 10 skipped`.
* **Binary timeline order centralized**: Commit `48518a5` replaced scattered binary parser `TimelineKind` pushes with `TimelineOrderBuilder`, including a debug assertion for registration-before-push mistakes. Focused `timeline_order` tests passed with `5 passed, 549 skipped`; full parity gate stayed green with `544 passed, 10 skipped`.
* **TrackEntry settings centralized**: Commit `e1e827f` moved entry settings application into `spine2d::TrackEntrySettings`, replaced Bevy's duplicate settings implementation with a type alias, deleted unused TrackEntry completion flags, and added coverage for negative delay plus `setMixDuration(mixDuration, delay)` queued-delay adjustment. Full core parity gate passed with `546 passed, 10 skipped`; Bevy nextest passed with `42 passed, 0 skipped`.
* **TrackEntry fields hidden**: Commit `fc1c241` made TrackEntry state private and exposed read-only getters for external use, matching the official C++ getter/setter shape more closely. Focused animation-state tests, Bevy nextest, Bevy check, and the full core parity gate all passed.
* **TrackEntry delay branch aligned**: Commit `f36cfa7` adjusted the new delay setter path to match `spine-cpp` branch behavior: negative delay is ignored or adjusted, while non-comparable values are not coerced through the queued-delay formula. The same focused and full gates stayed green.
* **TrackEntry control surface audit complete**: Final U5 setter audit found no additional guard changes needed; official `spine-cpp` TrackEntry setters are intentionally minimal except for negative delay handling.
* **Skeleton path scratch boundary started**: Commit `3edaa0b` moved path constraint scratch storage and capacity estimation into `skeleton::path`. Focused solver tests passed with `112 passed, 444 skipped`; full core parity gate passed with `546 passed, 10 skipped`.
* **Skeleton path world-position boundary moved**: Commit `0dab0fb` moved `path_attachment_for_slot`, `compute_path_world_positions`, and private path curve helpers into `skeleton::path`. `compute_attachment_world_vertices` stays in `skeleton.rs` for now because it is shared by path solving and `Skeleton::world_vertices`. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused solver nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton update-cache boundary extracted**: Commit `190a119` moved update-cache ordering helpers into `skeleton::cache`. This follows the official C++ split where `Skeleton::updateCache` drives ordering and constraints own `sort` behavior, adapted to Rust's centralized constraint storage. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused solver/cache nextest (`117 passed, 439 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton bone transform helpers extracted**: Commit `757b2f7` moved BonePose-equivalent world/local transform helpers and root/child world-transform math into `skeleton::bone`. This keeps `Bone` itself in `skeleton.rs` for now but aligns the matrix math boundary more closely with official `BonePose`. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused skeleton/upstream-ik demo/IK/transform/path/physics/slider nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton applied-transform helpers extracted**: Commit `a37abac` moved BonePose-equivalent `modifyWorld`, `modifyLocal`, child world reset, and applied-transform decomposition into `skeleton::bone`, leaving `Skeleton` as the caller-facing wrapper for existing constraint code. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused skeleton/upstream-ik demo/IK/transform/path/physics/slider nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton bone world-update entry extracted**: Commit `fc3ef3c` moved `update_bone_world_transform`'s implementation into `skeleton::bone`, leaving `Skeleton` with a thin wrapper used by the update cache dispatcher. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused skeleton/upstream-ik demo/IK/transform/path/physics/slider nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton IK solver helpers extracted**: Commit `e076419` moved the IK solver entry and helper routines into `skeleton::ik`, leaving `Skeleton` with a thin dispatcher wrapper. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused IK/solver nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton transform solver helpers extracted**: Commit `d772a9f` moved the transform constraint solver entry and helper routines into `skeleton::transform`, leaving `Skeleton` with a thin dispatcher wrapper. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused transform/solver nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton physics solver helpers extracted**: Commit `6be2f7b` moved the physics constraint solver entry and helper routines into `skeleton::physics`, leaving `Skeleton` with a thin dispatcher wrapper. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused physics/solver nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton slider solver helpers extracted**: Commit `6104586` moved the slider constraint solver entry and helper routines into `skeleton::slider`, leaving `Skeleton` with a thin dispatcher wrapper. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused slider/animation/solver nextest (`186 passed, 370 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton path apply entry extracted**: Commit `5e93794` moved the path constraint apply entry into `skeleton::path` and narrowed path-only helper visibility. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused path/solver nextest (`112 passed, 444 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton vertex world helper extracted**: Commit `7f98a3d` moved generic attachment world-vertices computation into `skeleton::vertices`, closer to the official `VertexAttachment::computeWorldVertices` responsibility boundary. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused vertices/path/render nextest (`166 passed, 390 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton Bone type extracted**: Commit `b712f53` moved the `Bone` runtime type into `skeleton::bone` and re-exported it from `skeleton`, preserving the external type path while matching the official separated `Bone/BonePose` boundary more closely. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused bone/skeleton nextest (`119 passed, 437 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton Slot type extracted**: Commit `6f56a26` moved the `Slot` runtime type into `skeleton::slot` and re-exported it from `skeleton`, preserving the external type path while matching the official separated `Slot` boundary more closely. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused slot/attachment/render nextest (`173 passed, 383 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton constraint runtime types extracted**: Commit `fcf3389` moved IK, transform, path, physics, and slider runtime constraint types into their matching `skeleton::*` modules and re-exported them from `skeleton`, preserving existing external type paths while matching the official separated constraint classes more closely. Verification stayed green: `cargo fmt --all --check`, `git diff --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused type/solver nextest (`195 passed, 361 skipped`), and full core parity nextest (`546 passed, 10 skipped`).
* **Skeleton public field surface narrowed**: Commit `047be09` made `Skeleton` container/state fields crate-visible and added public collection accessors plus official-style color, position, and scale setters. The diagnostic examples now use the accessor API. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton/API nextest (`196 passed, 361 skipped`), full core parity nextest (`547 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Bone public pose fields narrowed**: Commit `12218d2` made `Bone` local pose, applied pose, active state, and world-transform fields crate-visible, adding public accessors/setters that mirror official `BoneLocal` and `BonePose` control points. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton/Bone/API nextest (`197 passed, 361 skipped`), full core parity nextest (`548 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Slot public pose fields narrowed**: Commit `2643dd0` made `Slot` pose fields crate-visible, adding public accessors/setters that mirror official `SlotPose` color, dark-color, attachment, sequence, deform, and blend control points. Attachment-name changes now reset sequence and deform through the setter. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton/Slot/API nextest (`198 passed, 361 skipped`), full core parity nextest (`549 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Constraint public pose fields narrowed**: Commit `0c8d8cd` made IK, transform, path, physics, and slider runtime pose fields crate-visible, adding public accessors/setters that mirror the official C++ constraint pose/control surfaces. Physics integration scratch state is crate-visible only. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton/constraint/API nextest (`199 passed, 361 skipped`), full core parity nextest (`550 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Physics movement controls added**: Commit `ecdf83f` added `Skeleton::physics_translate`, `Skeleton::physics_rotate`, `PhysicsConstraint::translate`, and `PhysicsConstraint::rotate`, matching the official `spine-cpp` entry points and translate/rotate formulas. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton/physics nextest (`85 passed, 476 skipped`), full core parity nextest (`551 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Named lookup helpers added**: Commit `9bae119` added `Skeleton` root/find bone/slot helpers, slot-name attachment lookup, and source-skin-aware `set_attachment` semantics that no-op on missing names, matching the official `spine-cpp` convenience API shape more closely. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton nextest (`15 passed, 549 skipped`), full core parity nextest (`554 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Bounds helper added**: Commit `83df693` added a no-clipper `Skeleton::bounds` helper that computes an official-style AABB over active region and mesh attachments in draw order. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton nextest (`16 passed, 549 skipped`), full core parity nextest (`555 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Constraint lookup helpers added**: Commit `fed0975` added explicit `Skeleton` by-name get/mut helpers for IK, transform, path, physics, and slider constraints, matching the official `findConstraint<T>` convenience behavior without carrying C++ RTTI into Rust. Direct index lookup later became private implementation detail because latest-tag C++ does not expose public constraint-index lookup helpers. Verification stayed green: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu`, `cargo check -p spine2d-web`, focused skeleton nextest (`17 passed, 549 skipped`), full core parity nextest (`556 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Setup pose split added**: Commit `955cc27` added official-style `Skeleton::setup_pose`, `setup_pose_bones`, and `setup_pose_slots`, and fixed empty setup attachment `sequenceIndex` reset behavior to match `spine-cpp` `Slot::setupPose`. Verification stayed green: focused skeleton nextest (`18 passed, 549 skipped`), full core parity nextest (`557 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend checks, `cargo fmt --all --check`, and `git diff --check`.
* **Setup pose alias removed**: Commit `c385349` removed the legacy `Skeleton::set_to_setup_pose` alias as a breaking API cleanup; callers now use `Skeleton::setup_pose`. Verification stayed green across focused skeleton tests, full core parity (`557 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), Bevy/WGPU examples, web, formatting, and diff checks.
* **Clipping-aware bounds added**: Commit `43c5503` added `Skeleton::bounds_with_clipping`, matching the official `getBounds(..., SkeletonClipping*)` overload while preserving `bounds()` as the no-clipper default. Verification stayed green: focused skeleton/bounds/clipping nextest (`29 passed, 539 skipped`), full core parity (`558 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend checks, `cargo fmt --all --check`, and `git diff --check`.
* **Skeleton world/skin controls aligned**: Commit `aec70e4` made wind/gravity/time/update direct C++-style assignments, added component-level wind/gravity accessors, made missing skins no-op, and deleted `Error::UnknownSkin`. Verification stayed green: focused skeleton/skin/deform/render nextest (`86 passed, 482 skipped`), full core parity (`558 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Skeleton skin-result wrapper removed**: Commit `ae1ab99` deleted the now-useless `Skeleton::set_skin` `Result` wrapper and updated all call sites to the direct no-return setter. Verification stayed green with the same focused skeleton/skin/deform/render nextest (`86 passed, 482 skipped`), full core parity (`558 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Skeleton current skin accessor aligned**: Commit `ea3d166` changed `Skeleton::skin()` to return `Option<&SkinData>` like C++ `getSkin()`. The stored skin name is now kept internal, and a later follow-up removed the separate `skin_name()` accessor. Verification stayed green: focused skeleton accessor tests (`2 passed, 566 skipped`), full core parity (`558 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Skeleton update-cache public surface added**: Commit `f0903f1` exposed `UpdateCacheItem` and `Skeleton::update_cache_items()` as a read-only typed view over the solver cache, matching C++ `getUpdateCache()` without making the cache mutable. Verification stayed green: focused update-cache/accessor tests (`2 passed, 567 skipped`), full core parity (`559 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Skeleton ordered constraint public surface added**: Commit `295c836` exposed `ConstraintRef` and `Skeleton::constraints()` as a read-only typed view sorted by global constraint order, covering the Rust equivalent of C++ `getConstraints()`. Verification stayed green: focused constraint/update-cache tests (`2 passed, 568 skipped`), full core parity (`560 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **BonePose helper public surface added**: Commit `c20ab80` exposed `Bone::child_indices`, parent-space point transforms, local/world rotation transforms, and `rotate_world`, matching the remaining C++ `Bone`/`BonePose` helper surface in Rust's index-based storage model. Verification stayed green: focused Bone helper tests (`3 passed, 570 skipped`), full core parity (`563 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **BonePose update/local public surface added**: Commit `b2cadd4` exposed `Skeleton::update_bone_world_transform`, `update_bone_local_transform`, `validate_bone_local_transform`, `modify_bone_local`, and `modify_bone_world`, covering the C++ `BonePose` update/cache marker helpers without exposing the raw update counter. Verification stayed green: focused BonePose update tests (`3 passed, 573 skipped`), full core parity (`566 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **BonePose local-update epoch aligned**: Commit `e3e96c0` made `update_bone_local_transform` and `validate_bone_local_transform` keep the bone world epoch current after rebuilding applied local state, matching C++ `BonePose::updateLocalTransform`'s `_world = skeleton._update` side effect. The focused BonePose update tests now assert same-epoch world transforms are not clobbered by later applied-pose edits.
* **Bone y-down scale control added**: Commit `d374ddf` added `Bone::is_y_down`/`set_y_down`, made `Skeleton::get_scale_y()` and BonePose world/local transform math use the C++-style effective scaleY, and kept default `false` for the repo's Y-up oracle baseline. Verification stayed green: focused yDown test (`1 passed, 576 skipped`), full core parity (`567 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), backend and examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Hidden update-cache debug API removed**: Commit `71ddc60` deleted `Skeleton::debug_update_cache` and the private string formatter from `skeleton::cache`. The example and binary debug test now format the existing typed `update_cache_items()` view locally, keeping C++ `printUpdateCache()` as an internal/debug concern rather than a public Rust compatibility helper. Verification stayed green: focused update-cache nextest (`1 passed, 576 skipped`), full core parity (`567 passed, 10 skipped`), core/examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Attachment world-vertices helper documented**: Commit `9ea3c43` replaced hidden `Skeleton::slot_vertex_attachment_world_vertices` with documented `slot_attachment_world_vertices`, updating examples and oracle debug dumps. This preserves the needed Rust query while deleting the old hidden name and aligning the API wording with C++ `VertexAttachment::computeWorldVertices`. Verification stayed green: focused world-vertices/bounds/clipping nextest (`12 passed, 565 skipped`), full core parity (`567 passed, 10 skipped`), core/examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Applied draw order split**: Commit `26c4709` separated applied draw order from the unconstrained pose. `Skeleton::get_draw_order()` now reports the applied buffer for rendering and oracle dumps, `get_draw_order_pose()` keeps the pose buffer, and slider draw-order timelines only mutate the applied buffer during world transforms. Verification stayed green: focused draw-order/slider tests (`45 passed, 533 skipped`), full core parity (`568 passed, 10 skipped`), core/examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Applied slot pose split**: Commit `9147966` separated applied slot pose from the unconstrained slot pose. Slider-driven slot, attachment, sequence, and deform timelines now write to the applied `SlotPose` when the C++ runtime would, and render/oracle/world-vertex reads use the applied buffer. Verification stayed green: focused slot/draw-order/sequence/deform tests (`363 passed, 216 skipped`), full core parity (`569 passed, 10 skipped`), core/examples checks, `cargo fmt --all --check`, `git diff --check`, Bevy nextest (`42 passed, 0 skipped`), and backend example checks.
* **Rust-only slot attachment setter removed**: Commit `068263d` deleted `Slot::set_attachment_name`, which had no C++ runtime equivalent and could not preserve C++ `SlotPose::setAttachment(Attachment*)` timeline-attachment deform semantics from a bare name. Name-based runtime changes now go through `Skeleton::set_attachment`. Verification stayed green: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused slot/setup-pose nextest (`22 passed, 557 skipped`), and full core parity (`569 passed, 10 skipped`).
* **Rust-only slot bone setter removed**: Commit `7b83c3c` deleted `Slot::set_bone_index`. C++ exposes `Slot::getBone()` but does not allow runtime slot rebinding; Rust now keeps the slot bone index read-only outside crate internals. Verification stayed green: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused slot/accessor nextest (`21 passed, 558 skipped`), and full core parity (`569 passed, 10 skipped`).
* **Slot blend moved back to data**: Commit `9afff8b` removed runtime `Slot` blend storage and `Slot::blend/set_blend`. Render extraction now reads `SlotData.blend`, matching C++ where blend mode lives on `SlotData`, not `SlotPose`. Verification stayed green: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused slot/setup/render nextest (`37 passed, 542 skipped`), full core parity (`569 passed, 10 skipped`), and core examples check.
* **Attachment deform reset rule shared**: Commit `4f8b351` moved source-skin resolution and timeline-attachment comparison into `Skeleton` helpers. `Skeleton::set_attachment`, `set_skin` attachAll paths, and animation attachment timelines now use the same C++ `SlotPose::setAttachment` deform-clear rule, preserving deform when linked meshes share a timeline attachment. Verification stayed green: focused attachment/deform nextest (`79 passed, 503 skipped`), full core parity (`572 passed, 10 skipped`), core examples check, `cargo fmt --all --check`, and `git diff --check`.
* **Skin attachment helpers added**: Commit `b3ca6c1` added `SkinData::set_attachment`, `remove_attachment`, `names_for_slot`, `attachments_for_slot`, and `attachment_entries`, matching the official `Skin` attachment helper surface. `set_attachment` now grows slot storage like C++ `AttachmentMap::put`, and `add_skin` uses that shared path. Verification stayed green: focused skin nextest (`6 passed, 577 skipped`), full core parity (`573 passed, 10 skipped`), core examples check, `cargo fmt --all --check`, and `git diff --check`.
* **Skeleton attachment mutation return removed**: Commit `7cd8d8c` changed `Skeleton::set_attachment` to return `()` instead of a Rust-only success flag, matching C++ `Skeleton::setAttachment` void/no-op-on-miss semantics. Verification stayed green: focused skeleton attachment tests (`33 passed, 550 skipped`), full core parity (`573 passed, 10 skipped`), core examples check, `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState mix setters aligned**: Commit `0c78468` removed Rust-only mix duration validation from `AnimationStateData::set_default_mix`, `set_mix`, `set_empty_animation`, and `set_empty_animations`; these now follow C++ direct assignment semantics. Verification stayed green: focused animation-state tests (`7 passed, 576 skipped`), full core parity (`573 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), Bevy examples check, `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState empty queue API made infallible**: Commit `0a4204a` made `AnimationState::add_empty_animation` return `TrackEntryHandle` directly and removed its Rust-only finite-delay guard, matching C++ `addEmptyAnimation`. Verification stayed green: focused animation-state/Bevy tests, full core parity (`574 passed, 10 skipped`), Bevy nextest (`42 passed, 0 skipped`), Bevy examples check, `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState reverse-time updates aligned**: Commit `f381dc5` removed the Rust-only `AnimationState::update` delta guard and stopped Bevy from clamping scaled frame delta, matching C++ direct update semantics and preserving negative time-scale playback. Verification stayed green: focused animation-state tests (`57 passed, 528 skipped`), full core parity (`575 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **IK mix propagation aligned**: Commit `65c078f` removed the Rust-only finite/positive mix guards from one-bone and two-bone IK application so negative and NaN mix values propagate through the same C++ math path. Verification stayed green: focused IK tests (`5 passed, 582 skipped`), full core parity (`577 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Timeline alpha propagation aligned**: Commit `64679c2` removed Rust-only nonpositive-alpha guards and clamps from `AnimationState` current-entry apply, IK/transform/path/physics/slider timelines, slot color/two-color helpers, and deform timelines. New negative-alpha tests cover state, IK, transform, path, physics, slider, slot color/RGBA2, and deform paths. Verification stayed green: focused alpha tests (`8 passed, 589 skipped`), full core parity (`587 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Mix interpolation extension removed**: Commit `2f7dcb4` deleted the Rust-only `MixInterpolation` enum, `TrackEntry::mix_interpolation`, `TrackEntryHandle::set_mix_interpolation`, and `TrackEntrySettings::with_mix_interpolation`. Local `repo-ref/spine-runtimes/spine-cpp` has no matching `MixInterpolation`/`setMixInterpolation` surface, so mix percentage returned to the official linear `mixTime / mixDuration` path. Verification stayed green: focused animation-state tests (`56 passed, 538 skipped`), full core parity (`584 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **Mix completion comparisons tightened**: Commit `fce1ccc` changed queued-entry activation, track-end clearing, mixing completion, mixing-from attachment/draw-order gates, attachment alpha gates, and event thresholds to use direct comparisons instead of Rust-only epsilon padding. A direct `interruptAlpha` rewrite was evaluated and rejected because oracle hold-mix/interrupt scenarios regressed; keep the current Rust alpha compensation until the full C++ timeline-mode model is ported. Verification stayed green: focused oracle subset (`6 passed, 588 skipped`), full core parity (`584 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), examples checks, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry mixBlend API restored**: Commit `1a432d3` superseded the older development-tag additive assumption and aligned the public TrackEntry surface with local `repo-ref/spine-runtimes` C++: `mix_blend`, `set_mix_blend(MixBlend)`, `with_mix_blend(MixBlend)`, Bevy snapshots, render scenarios, and `pose_dump_scenario --entry-mix-blend` now use official `MixBlend`. The Rust-only `additive` public API and CLI flag were removed. Verification passed: focused mixing tests (`8 passed, 586 skipped`), oracle scenarios (`312 passed, 282 skipped`), full core parity (`584 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy examples checks, `cargo fmt --all`, and `git diff --check`.
* **C++ oracle mixBlend commands aligned**: Commit `9bb858f` updated pose/render C++ oracle helpers and golden recorders from the stale `--entry-additive` / `spine_track_entry_set_additive` path to official `--entry-mix-blend <setup|first|replace|add>` and `spine_track_entry_set_mix_blend`. The runners now have an explicit `SPINE2D_ORACLE_ALLOW_BASELINE_MISMATCH=1` override for intentionally using local `repo-ref/spine-runtimes` as the source checkout. Verification passed: parser smoke for `set_mix_blend(... MixBlend::Add)`, Python byte-compile, zsh syntax check, diff check, pose oracle run, render oracle run, and JSON validation for both temp outputs.
* **Worktree contention noted**: After `1a432d3`/`ebb47d9`, unknown-source Rust/Bevy edits appeared that revert the committed `mixBlend` public surface back to the older additive API. These files were not staged into `9bb858f`; do not overwrite or revert them without user confirmation.
* **Additive rollback cleared**: User confirmed the remaining dirty files were safe to handle. The 12 Rust/Bevy files that had reverted `mixBlend` back to the old additive API were restored to the committed `mixBlend` state; `git status --short` is clean.

## 2026-06-27
* **SkeletonData header/default-skin surface tightened**: Added C++-style `SkeletonData` header setters and changed default-skin handling from repeated `"default"` name lookup to an explicit stored default-skin slot. JSON marks the skin named `default` as default like `SkeletonJson`; binary marks the default-skin block only when present like `SkeletonBinary`; manual code uses `set_default_skin(Option<&SkinData>)`, avoiding public exposure of the internal index. Runtime attachment fallback, source-skin tracking, and path update-cache sorting now route through that explicit default skin. Verification passed: formatting, core check, examples check, tests check, focused default-skin/attachment nextest, full clean-target core nextest (`660 passed, 2 skipped`), and `git diff --check`.

## 2026-06-24
* **Session Handoff**: 2026-06-25 checkpoint: Bone child view was deepened to match latest-tag spine-cpp more closely. Replaced Rust-only child_indices index view with parent()/children() object-view helpers on runtime::Bone, and updated skeleton tests to assert parent/children relationships via Bone objects. Verified with cargo fmt --all -- --check, cargo check -p spine2d --features json,binary,upstream-smoke, and cargo nextest run -p spine2d --features json,binary,upstream-smoke model_lookup_tests runtime::skeleton_tests --no-fail-fast --status-level fail (35 passed, 613 skipped).
* **Session Handoff**: 2026-06-25 checkpoint: removed Rust-only convenience surface from spine2d model layer. BoneData keeps setup-pose fields and relationship lookup; SlotData keeps setup-pose attachment/blend/visible surface without fake slot-local skinRequired or extra color helpers; ConstraintDataRef now stays a thin unified view with internal sorting handled by get_constraints. Verified with cargo fmt --all -- --check, cargo check -p spine2d --features json,binary,upstream-smoke, and cargo nextest run -p spine2d --features json,binary,upstream-smoke model_lookup_tests --no-fail-fast --status-level fail (6 passed, 642 skipped).
* **Binary debug dumps removed**: Deleted the ignored `debug_dump_*` pose/constraint helpers from `spine2d/src/binary_tests.rs` and the shared `update_cache_debug_labels` helper they alone used. The remaining binary tests are still the same behavior checks, but the file no longer carries manual-output diagnostics that were only useful during early `.skel` vs JSON debugging. Verification passed: formatting, focused atlas nextest (`10 passed, 60 skipped`), focused render nextest (`26 passed, 629 skipped`), isolated full feature nextest (`645 passed, 2 skipped`), feature check, WGPU/Web check with the existing `block v0.1.6` warning, Bevy examples check, and default check/nextest (`70 passed`).
* **Atlas region lookup renamed**: Replaced the Rust-only `Atlas::region(name)` helper with `Atlas::find_region(name)`, the Rust naming analogue of C++ `Atlas::findRegion`. All repo call sites now use the C++-style lookup name. Verification passed: formatting, focused atlas nextest (`10 passed, 60 skipped`), focused render nextest (`26 passed, 629 skipped`), feature check, WGPU/Web check with the existing `block v0.1.6` warning, Bevy examples check, default check/nextest (`70 passed`), and full feature core nextest (`645 passed, 10 skipped`).
* **Rust-only Atlas page helper removed**: Deleted `Atlas::page(index)`, which had no latest-tag C++ counterpart; callers should use the public `Atlas.pages` vector, the Rust analogue of C++ `Atlas::getPages()`. Internal render code now reads pages with `atlas.pages.get(region.page)`. Verification passed: formatting, focused atlas nextest (`10 passed, 60 skipped`), focused render nextest (`26 passed, 629 skipped`), feature check, WGPU/Web check with the existing `block v0.1.6` warning, Bevy examples check, default check/nextest (`70 passed`), and full feature core nextest (`645 passed, 10 skipped`).
* **Rust-only AtlasPage scale removed**: Deleted the public `AtlasPage.scale` field and the `scale:` page parser branch after rechecking latest-tag `spine-cpp`: `AtlasPage` has no scale field, and `Atlas.cpp` ignores page-level `scale` entries. This is a breaking cleanup; atlas text containing `scale:` still parses because unknown page entries are skipped. Verification passed: formatting, focused atlas nextest (`10 passed, 60 skipped`), feature check, WGPU/Web check with the existing `block v0.1.6` warning, Bevy examples check, default check/nextest (`70 passed`), and full feature core nextest (`645 passed, 10 skipped`).
* **Atlas page enum surface aligned**: Removed the Rust-only `AtlasFilter::Other(String)` preservation path and added C++-style `AtlasFilter::Unknown`; unknown filter names now collapse to `Unknown` like `spine-cpp`'s `TextureFilter_Unknown`. `AtlasWrap::MirroredRepeat` was added to cover the official wrap enum and WGPU sampler mapping now handles it. Verification passed: formatting, focused atlas nextest (`10 passed, 60 skipped`), feature check, WGPU/Web check with the existing `block v0.1.6` warning, default check/nextest (`70 passed`), and full feature core nextest (`645 passed, 10 skipped`).
* **Atlas page texture path exposed**: `AtlasPage` now stores `texture_path`, matching C++ `AtlasPage::texturePath` for the string-parsing constructor with an empty directory. Core draw extraction now uses `page.texture_path` instead of `page.name` as the emitted texture identifier, while existing callers still observe the same value because `Atlas::parse` has no directory parameter yet. Verification passed: formatting, focused atlas nextest (`9 passed, 60 skipped`), focused render nextest (`26 passed, 628 skipped`), default check/nextest (`69 passed`), feature check, and full feature core nextest (`644 passed, 10 skipped`).
* **Atlas packed dimensions exposed**: `AtlasRegion` now stores C++ `getPackedWidth()` / `getPackedHeight()` semantics as `packed_width` and `packed_height`. Ninety-degree rotated regions compute UVs from the atlas-entry width/height first, then swap the packed dimensions like `spine-cpp`; region and mesh render paths now consume those packed dimensions for the C++ `RegionAttachment` and `MeshAttachment` trim formulas. Verification passed: formatting, focused atlas nextest (`9 passed, 60 skipped`), focused render nextest (`26 passed, 628 skipped`), default check/nextest (`69 passed`), feature check, and full feature core nextest (`644 passed, 10 skipped`).
* **Atlas TextureRegion fields and flipV exposed**: `AtlasRegion` now stores parsed `u`, `v`, `u2`, `v2`, `region_width`, and `region_height` like the C++ `TextureRegion` base, and `Atlas::flip_v` mirrors C++ `Atlas::flipV` by flipping each region's v coordinates. Region and mesh render paths now consume the stored UV fields, so flipped atlases affect draw output. Verification passed: focused atlas nextest (`9 passed, 60 skipped`), focused render nextest (`26 passed, 628 skipped`), feature check, and full feature core nextest (`644 passed, 10 skipped`).
* **Atlas rotate flag exposed**: `AtlasRegion` now stores the C++ `AtlasRegion::getRotate()` boolean alongside `degrees`, with `rotate` true only when parsed degrees are exactly `90`. This preserves the official distinction where `rotate: 180` / `270` have nonzero degrees but `getRotate()` remains false. Verification passed: formatting, focused default atlas nextest (`8 passed, 60 skipped`), feature check, and full feature core nextest (`642 passed, 10 skipped`).
* **Parser-only helpers cfg-scoped**: `ScaleYMode::from_name`, version-prefix matching, `runtime::finalize_animation`, and `timeline_order_for_animation` now compile only for JSON/Binary parser surfaces or tests that construct manual animations. This keeps the default core build from carrying unused parser helpers while preserving the full feature parity path. Verification passed: formatting, focused default atlas nextest (`8 passed, 60 skipped`), feature check, and full feature core nextest (`642 passed, 10 skipped`).
* **Dead weighted-mesh error removed**: Deleted `Error::JsonUnsupportedWeightedMesh`, a stale JSON error variant from before weighted meshes were implemented. JSON now parses weighted mesh vertices through `parse_weighted_mesh_vertices`, binary and render paths have focused weighted coverage, and no live code constructed the old unsupported-error variant.
* **Atlas region surface aligned**: `Atlas.regions` now mirrors C++ `Atlas::getRegions()` as a parse-order array instead of a Rust `HashMap`, and `Atlas::region(name)` scans by name like `Atlas::findRegion`. The parser now follows the C++ page/region loop shape, keeps `AtlasPage.index` and `AtlasPage.format`, stores `AtlasRegion.index`, preserves unknown region name/value entries such as `split` and `pad`, uses C++-style `rotate` integer parsing, and keeps page/region test fixtures in official atlas layout. Verification passed: focused atlas unit tests (`8 passed`), focused render atlas nextest (`9 passed, 643 skipped`), `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and full feature core nextest (`642 passed, 10 skipped`).
* **SkeletonData ordered skin/event storage restored**: Historical note: this 2026-06-24 slice moved `SkeletonData.skins` and `SkeletonData.events` from `HashMap` to ordered `IndexMap` storage to preserve C++ `SkeletonData::getSkins()` / `getEvents()` array order for manual construction, JSON parsing, and binary parsing. That intermediate public shape was superseded on 2026-06-26 by C++-style ordered `Vec` storage plus slice getters.
* **SkeletonData unified constraints exposed**: `SkeletonData::constraints()` now covers latest-tag C++ `SkeletonData::getConstraints()` with a typed Rust view over IK, transform, path, physics, and slider constraint data. The parser still stores typed vectors internally, but the public view reconstructs unified constraint order from each data object's parser-assigned `order`; JSON mixed-type constraint parsing is covered by a focused regression. Verification passed: focused model lookup nextest (`3 passed, 646 skipped`), core check, formatting, diff check, and full core nextest (`639 passed, 10 skipped`).
* **PointAttachment world rotation aligned**: `PointAttachmentData::compute_world_rotation` now matches latest-tag `spine-cpp` `PointAttachment::computeWorldRotation` by rotating the bone matrix with the attachment's local rotation before taking `atan2`. This fixes the non-uniform-scale case where the old Rust formula only matched pure rotation. Verification passed on 2026-06-24: focused point-attachment nextest (`2 passed, 646 skipped`) and full core nextest (`638 passed, 10 skipped`).
* **Animation timeline view unified**: `Animation::timeline_order` is now crate-internal, and `Animation::get_timelines()` exposes a C++-style unified iterator over parse-order timelines, with event timelines reported last like the official `_timelines` list. `pose_dump_scenario` now renders timeline-order diagnostics through that public iterator instead of reading the raw field. Verification passed on 2026-06-24: `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke json_timeline_order_tests --status-level fail` (`3 passed, 627 skipped`), `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_mixing_semantics_tests --status-level fail` (`16 passed, 614 skipped`), and `cargo check -p spine2d --features json --example pose_dump_scenario`.
* **TrackAnimationInput overload bridge deleted**: `TrackAnimationInput` and the `#[allow(private_bounds)]` public generic bridge were removed. `AnimationState::set_animation` / `add_animation` now cover C++ name overload semantics directly, and `set_animation_ref` / `add_animation_ref` cover the C++ `Animation&` object path while preserving supplied animation identity. Verification passed on 2026-06-24: core check, Web/WGPU check, Bevy examples check, focused animation-reference preservation nextest, and focused mixing semantics nextest.
* **AnimationState computeHold parity tightened**: Rechecked Rust `compute_hold` against latest-tag C++ `computeHold` / `from`. The implementation now keeps `property_ids` scoped like C++ `_propertyIDs`, derives additive/instant/property-overlap checks through local helpers equivalent to `Timeline::getAdditive`, `Timeline::getInstant`, and `Animation::hasTimeline(ids)`, and removed the Rust-only hold-mix clamp so hold alpha uses direct `1 - mix`. Existing queued-entry promotion and `Start` event paths were confirmed already aligned. Verification passed: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and focused mixing semantics (`16 passed, 614 skipped`).
* **AnimationState queue event ordering aligned**: Normal `set_animation` replacement now queues discarded-entry `Dispose` events before interrupting the old current entry, matching C++ `clearNext(current)` before `setTrack`. `clear_track` now queues discarded next-entry disposals before ending the mixing-from chain, matching C++ `clearTrack`. Verification passed: focused replacement/clear-track regressions, `runtime::animation_state_tests` (`75 passed, 557 skipped`), and focused mixing semantics (`16 passed, 616 skipped`).
* **Slot setup pose now carries sequence state**: `SlotSetupPose` was widened to keep the setup sequence index alongside color and dark color, and `Skeleton::new` / `setup_pose_slots` now preserve that setup sequence state while matching C++ slot setup behavior. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and focused nextest (`52 passed, 596 skipped`).
* **HoldPrevious surface restored**: Commit `e25ca6e` mirrors official C++ `TrackEntry::getHoldPrevious/setHoldPrevious` in Rust, Bevy, examples, and C++ lite oracle helpers. Verification passed: focused mixing semantics (`10 passed, 586 skipped`), full core parity (`586 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy example checks, parser smoke, Python byte-compile, zsh syntax checks, pose/render oracle runs with `--entry-hold-previous 1`, temp JSON validation, `cargo fmt --all --check`, and `git diff --check`. Stale additive/holdPrevious plan files `2026-06-18-006`, `-010`, `-011`, and `-014` were deleted because they contradicted the local C++ reference.
* **Stale TrackEntry parity plans pruned**: The obsolete development-branch plans `2026-06-18-001`, `-009`, `-012`, and `-013` were deleted after `mixBlend` and `holdPrevious` became the current C++-aligned public surface. Remaining docs now point at the active C++ parity hardening plan instead of the removed `additive` / `mixInterpolation` cleanup plan, and the stale additive-oriented test name was renamed to `track_entry_mix_blend_add_adds_current_pose`.
* **TrackEntry query helpers exposed**: Commit `866b732` added official-style `TrackEntry::is_complete`, `TrackEntry::was_applied`, `TrackEntry::is_empty_animation`, `TrackEntryHandle::set_loop`, and `TrackEntrySettings::with_looped`, matching C++ `isComplete`, `wasApplied`, `isEmptyAnimation`, and `setLoop` without changing default playback behavior. Verification passed: focused core query-helper test (`1 passed, 596 skipped`), focused Bevy settings test (`1 passed, 42 skipped`), core animation_state tests (`59 passed, 538 skipped`), full core parity (`587 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy checks, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry queue neighbors exposed**: Commit `8c074f6` added safe-handle equivalents for C++ `TrackEntry::getPrevious`, `getNext`, and `isNextReady`: `TrackEntryHandle::previous`, `next`, and `is_next_ready`. The implementation reconstructs the official current-plus-queued chain from `AnimationState` without adding persistent queue pointers. Verification passed: focused queue-neighbor test (`1 passed, 597 skipped`), full core parity (`588 passed, 10 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry links and time setters aligned**: Commit `cfd01fe` replaced the temporary reconstructed previous/next view with explicit C++-style `TrackEntry` previous/next storage, preserving the old mixing-from entry's next pointer when a queued entry becomes current and clearing links when queued entries are discarded. It also added `TrackEntryHandle::mixing_from`, `mixing_to`, `set_track_time`, and `set_mix_time`, matching C++ `getMixingFrom`, `getMixingTo`, `setTrackTime`, and `setMixTime`. Verification passed: focused query/link tests (`2 passed, 596 skipped`), full core parity (`588 passed, 10 skipped`), Bevy check, `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState apply status aligned**: Commit `d39573a` changed `AnimationState::apply` to return `bool`, matching C++ `AnimationState::apply(Skeleton&)` returning true only when at least one non-delayed current track is applied. Verification passed: focused apply-status test (`1 passed, 598 skipped`), full core parity (`589 passed, 10 skipped`), Bevy check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState current/data accessors added**: Commit `8a46380` added safe Rust equivalents for C++ `getCurrent` and `getData`: `AnimationState::get_current(track_index)` returns an optional `TrackEntryHandle`, and `AnimationState::get_data()` pairs with `get_data_mut()`. Verification passed: focused current/data tests (`2 passed, 599 skipped`), full core parity (`591 passed, 10 skipped`), Bevy check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState event queue controls added**: Commit `f6b88fe` exposed `disable_queue` and `enable_queue`, matching C++ `disableQueue` / `enableQueue`. The focused test verifies disabled queues suppress listener delivery, `enable_queue` itself does not drain, and the queued event drains on the next normal update point. Verification passed: focused queue/current/data tests (`3 passed, 599 skipped`), full core parity (`592 passed, 10 skipped`), Bevy check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Manual TrackEntry disposal added**: Commit `d0ada6c` added optional `set_manual_track_entry_disposal`, `get_manual_track_entry_disposal`, and `dispose_track_entry`, matching C++ manual disposal controls while preserving automatic disposal by default. Verification passed: focused manual-disposal and queue tests (`2 passed, 601 skipped`), full core parity (`593 passed, 10 skipped`), Bevy check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **AnimationState track slots view added**: Commit `ca658f2` added `AnimationState::get_tracks() -> Vec<Option<TrackEntryHandle>>`, a safe sparse view equivalent to C++ `getTracks()` for current track slots. Verification passed: focused current/tracks test (`1 passed, 602 skipped`), full core parity (`593 passed, 10 skipped`), Bevy check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **AnimationStateData skeleton accessor aligned**: Commit `c2147b3` made `AnimationStateData::skeleton_data` private and added `get_skeleton_data() -> &SkeletonData`, matching the C++ `getSkeletonData()` getter shape without exposing Rust ownership internals. Verification passed: focused accessor test (`1 passed, 603 skipped`), full core parity (`594 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **AnimationStateData animation-reference mix accessors added**: Commit `1f4dd8c` added `AnimationStateData::set_mix_animation` and `get_mix`, covering the C++ `setMix(Animation&, Animation&)` / `getMix(Animation&, Animation&)` overloads while reusing the existing name-indexed mix table. Verification passed: focused animation-mix test (`1 passed, 604 skipped`), full core parity (`595 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntry handles bound to AnimationState**: Commit `c30b446` added an owner id to `TrackEntryHandle` and routes handle reads/writes through owner checks so cross-state misuse is rejected. The actual payload lookup remains `TrackEntryHandle::entry(&state)`; the earlier idea of reusing `get_animation_state` for that lookup was rejected because it would blur the handle/owner split. Verification passed: focused cross-state handle test (`1 passed, 605 skipped`), full core parity (`596 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **TrackEntryHandle read accessor added**: Commit `3710686` added `TrackEntryHandle::with_entry`, letting callers read current or queued entry getters through a state-bound handle while rejecting cross-state misuse. Verification passed: focused handle read test (`1 passed, 606 skipped`), full core parity (`597 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check`.
* **Rust-only mix removal APIs deleted**: Breaking commit `c5eb82c` removed `AnimationStateData::pair_mix`, `AnimationStateData::remove_mix`, Bevy `SpineAnimationCommand::remove_mix`, and the `RemoveMix` command variant because local `spine-cpp` exposes only `setMix`, `getMix`, and `clear` for `AnimationStateData`. Tests now cover explicit mix overwrite plus clear/reset behavior. Verification passed: focused core mix tests (`3 passed, 604 skipped`), focused Bevy mix command test (`1 passed, 42 skipped`), full core parity (`597 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Track snapshot convenience APIs deleted**: Breaking commit `9c206bf` removed `AnimationState::tracks_len` and `AnimationState::track_snapshots`, and Bevy runtime-state sync now reads `get_tracks()` plus state-bound handles directly. Verification passed: full core parity (`597 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Track-entry closure convenience APIs deleted**: Breaking commit `ac9fcb8` removed public `AnimationState::with_track_entry` and `AnimationState::with_queued_track_entry`. Tests and Bevy helpers now use `get_current()`, `TrackEntryHandle::next()`, and `TrackEntryHandle::entry(&state)` so the public surface stays closer to C++ `getCurrent` plus `TrackEntry` links. Verification passed: core check, full core parity (`597 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Name-based mix getter deleted**: Breaking commit `472bb75` removed `AnimationStateData::get_mix(&str, &str)` because local `spine-cpp` exposes `setMix` by name but only `getMix(Animation&, Animation&)` for reads. Tests now resolve animations and assert through `get_mix`. Verification passed: focused AnimationStateData tests (`5 passed, 602 skipped`), full core parity (`597 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), Bevy examples check, WGPU/Web check, `cargo fmt --all --check`, and `git diff --check`.
* **Pose scenario threshold CLI completed**: `spine2d/examples/pose_dump_scenario.rs` now accepts `--entry-event-threshold` and `--entry-alpha-attachment-threshold`, matching the C++ pose oracle and Rust/C++ render scenario command surface. Verification passed: `cargo build -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, a threshold-command JSON smoke, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, and `git diff --check`.
* **Bevy setup-pose command renamed**: The Bevy wrapper deleted the stale `SpineSkeletonCommand::reset_to_setup_pose` / `SpineSkeletonCommandKind::ResetToSetupPose` spellings and now exposes `setup_pose` / `SetupPose`, matching core `Skeleton::setup_pose` and C++ `setupPose`. Verification passed: no old spelling remains under `spine2d-bevy`, Bevy examples compile, and Bevy nextest reports `43 passed, 0 skipped`.
* **Rust-only Atlas parser alias removed**: Deleted the inherent `Atlas::from_str` alias and migrated repo callers to `Atlas::parse`, while keeping the standard `FromStr` implementation for idiomatic `"text".parse::<Atlas>()` callers. This removes a duplicate public entry point that had no C++ runtime counterpart. Verification passed: no `Atlas::from_str` or `should_implement_trait` references remain, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, and `cargo check -p spine2d-wgpu -p spine2d-web` (with the existing `block v0.1.6` warning).
* **Internal mix-direction helper narrowed**: Changed `MixDirection` from public to crate-visible so the runtime keeps the internal mix-in/mix-out dispatch helper without exposing a stale public type that does not exist on the latest C++ surface. Verification passed: core check, `cargo fmt --all --check`, and `git diff --check`.
* **Standalone sampling helper made test-only**: Marked `apply_animation` as test-only so the non-test crate surface keeps the runtime helper split without exporting a redundant standalone sampling entry point. Verification passed: core check, focused runtime sampling tests, `cargo fmt --all --check`, and `git diff --check`.
* **Internal mix-blend helper narrowed**: Changed `MixBlend` from public to crate-visible. It remains an internal representation for timeline application, while public latest-tag mixing control stays on `TrackEntry::additive` and `MixInterpolation`. Verification passed: `cargo check -p spine2d --features json,binary,upstream-smoke`.
* **Ordered constraint wrapper retained after C++ recheck**: Rechecked latest-tag `Skeleton::getConstraints()` and kept public `ConstraintRef` / `Skeleton::constraints()` as the safe Rust analogue for the unified constraint view. Verification passed: core check and focused `runtime::skeleton_tests`.
* **TrackEntry settings value object moved out of core**: Deleted public `spine2d::TrackEntrySettings` and kept entry configuration on C++-style `TrackEntryHandle` setters. Bevy now owns `SpineTrackEntrySettings` locally for ECS command batching. Verification passed: core check, focused `runtime::animation_state_mixing_semantics_tests` (`16 passed, 613 skipped`), Bevy examples check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
* **Latest-tag audit docs normalized**: Rechecked additive/instant timeline classification plus the remaining `AnimationStateData` / `TrackEntry` public surface against pinned latest-tag `spine-cpp` and found no runtime mismatch requiring code changes. Rust's classification matches the C++ `_additive` / `_instant` timeline flags for represented timeline kinds, `AnimationStateData` mix keys remain name-based like C++ `AnimationPair`, and the delay/mix-duration handle branches already match C++. The stale beta audit entry points were deleted, the active audit map was renamed to `docs/upstream-audit-4.3-latest.md`, and `scripts/check_spine_baseline.py` now validates that current latest-tag doc. Verification passed: `python3 scripts/check_spine_baseline.py --verify-local` and `git diff --check`.
* **Spine version gate tightened to C++ prefix semantics**: JSON and binary parsing now reject any export version string that does not start with `4.3`, matching the official C++ `String::startsWith(SPINE_VERSION_STRING)` check instead of the previous Rust-only major-version leniency. The JSON upstream smoke suite now skips non-4.3 examples before parsing so the intended 4.2 fixtures remain non-gates, and new regression tests cover both `4.4.00` and missing `skeleton.spine` / binary version headers. Verification passed: `cargo test -p spine2d --features json,binary,upstream-smoke --lib version_tests -- --nocapture`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`627 passed, 10 skipped`), `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`), `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, `git diff --check`, and baseline verification.
* **Skin / Attachment surface rechecked**: Rechecked latest-tag `spine-cpp` `Skin` attachment helper behavior against Rust `SkinData`. No code change was needed: growable slot storage, replace-in-place behavior, insertion-order iteration, skin merge semantics, and `attachAll` behavior are already covered by the current implementation, focused tests, and parity checklist. This area is treated as aligned unless a new oracle delta appears.
* **Zero-arg Skeleton world-transform helper deleted**: The Rust-only `Skeleton::update_world_transform()` convenience wrapper was removed after rechecking latest-tag `spine-cpp`, which only exposes `updateWorldTransform(Physics)`. Repo callers now pass `Physics::None` explicitly when they want the old default behavior, while physics-enabled paths remain explicit. Verification passed: `cargo fmt --all`, no remaining code references to `update_world_transform()`, core check, WGPU/Web check, Bevy examples check, full core parity (`627 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
* **SkeletonData lookup helpers aligned**: Deleted the Rust-only tuple-returning `SkeletonData::animation(name)` helper and replaced call sites with `SkeletonData::find_animation(name) -> Option<&Animation>`, matching C++ `SkeletonData::findAnimation`. `SkeletonData::skin(name)` was renamed to `find_skin(name)` to match C++ `findSkin`, while `Skeleton::skin()` remains the current-instance skin accessor. The pose dump keeps its animation index diagnostic by finding the animation by name. Verification passed: core check, WGPU/Web check, Bevy examples check, full core parity (`627 passed, 10 skipped`), Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and no remaining code references to the removed lookup helpers.
* **Skeleton index lookup helpers removed**: Deleted Rust-only public `Skeleton::find_bone_index`, `find_slot_index`, and the typed `find_*_constraint_index` helpers. Public lookup now follows latest-tag C++ by returning objects through `find_bone`, `find_slot`, and typed `find_*_constraint` methods; internal vector indexing uses private `*_index_by_name` helpers. Verification passed: `cargo fmt --all --check`, no remaining code references to the removed public helpers, core check, focused skeleton finder nextest (`2 passed, 635 skipped`), and full core nextest (`627 passed, 10 skipped`).
* **Skin color and copy surface aligned**: `SkinData` now stores the latest-tag `spine-cpp` `Skin::getColor()` state with the official constructor default, JSON array-form skins parse optional `color`, binary named skins preserve nonessential skin color, and `SkinData::copy_skin` mirrors C++ `copySkin` for Rust's owned-value attachment model. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused skin nextest (`7 passed, 632 skipped`), and full core nextest (`629 passed, 10 skipped`).
* **SkeletonData header metadata preserved**: Rechecked latest-tag `spine-cpp` `SkeletonData` / `SkeletonJson` / `SkeletonBinary` and added the missing Rust model fields for skeleton header metadata. JSON and binary readers now preserve `hash`, bounds, `reference_scale`, `fps`, `images_path`, and `audio_path`; `.skel` hashes follow C++ signed 64-bit stringification with zero mapped to empty, bounds stay unscaled, and `reference_scale` is the only header scalar multiplied by loader scale. `name` is present and defaults empty for the current string/bytes loaders because Rust has no path-based loader entry point yet. Verification passed: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused JSON/binary header nextest, focused parser nextest (`6 passed, 634 skipped`), and full core nextest (`630 passed, 10 skipped`).
* **SkeletonData default skin and slider animation helpers aligned**: Added C++-style `default_skin()` and `find_slider_animations(&mut Vec<&Animation>)` helpers. The default skin helper returns the skin named `default` when present, matching `SkeletonData::getDefaultSkin`, and the slider animation helper appends referenced animations into a caller buffer like C++ `findSliderAnimations`. `Skeleton` attachment lookup and source-skin resolution now route through `default_skin()` instead of repeated `"default"` lookups. Verification passed: focused slots/slider nextest (`3 passed, 639 skipped`) and full core nextest (`632 passed, 10 skipped`).
* **SkeletonData named data lookups aligned**: Added data-level lookup helpers for bones, slots, events, and every typed constraint vector: `find_bone`, `find_slot`, `find_event`, `find_ik_constraint`, `find_transform_constraint`, `find_path_constraint`, `find_physics_constraint`, and `find_slider_constraint`. These are the Rust equivalents of C++ `SkeletonData::findBone`, `findSlot`, `findEvent`, and `findConstraint<T>` while preserving the current typed-vector storage. `find_animation`, `find_skin`, and `find_event` now scan stored object names like C++ `findWithName`, so they no longer depend on Rust-only map keys or `animation_index` staying synchronized. Verification passed: focused model lookup nextest (`1 passed, 642 skipped`), `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and full core nextest (`633 passed, 10 skipped`).
* **Non-render attachment colors aligned**: Added color storage for point, path, bounding box, and clipping attachments, using the constructor defaults visible in latest-tag `spine-cpp`. JSON now preserves explicit colors and applies the type-specific defaults when absent, while binary keeps the nonessential colors it previously read and discarded. Verification passed: focused attachment/animation color nextest (`4 passed, 643 skipped`), `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and full core nextest (`637 passed (1 leaky), 10 skipped`).
* **Animation color field aligned**: Added `Animation::color` to preserve the C++ `Animation::getColor()` field. JSON animation objects now parse the optional `color` string, binary animations read the trailing nonessential RGBA, and the model defaults to white when the field is absent. Verification passed: focused JSON/Binary color nextest (`2 passed, 643 skipped`), `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and full core nextest (`635 passed, 10 skipped`).
* **Animation affected-bone query aligned**: Added `Animation::get_bones()` and `BoneTimeline::get_bone_index()` to cover the C++ `Animation::getBones()` / `BoneTimeline::getBoneIndex()` public query shape. Rust returns unique first-seen bone indices from the stored bone timelines, avoiding a duplicated cache while matching the affected-bone use case. Slider initialization now reuses the helper instead of keeping a sorted/deduped local collector. Verification passed: focused model lookup nextest (`2 passed, 642 skipped`), `cargo fmt --all -- --check`, and full core nextest (`634 passed, 10 skipped`).
* **TrackEntry value getters aligned**: Breaking cleanup renamed the remaining `TrackEntry` Rust-style value readers to latest-tag C++ getter spelling, including `get_track_index`, `get_animation`, `get_loop`, `get_additive`, `get_reverse`, `get_shortest_rotation`, timing getters, threshold getters, `get_animation_time`, and `get_track_complete`. Repo call sites in core tests and Bevy now use the C++-style getter names. Verification passed: focused core nextest (`animation_state_tests render_oracle_parity_tests upstream_spine_c_smoke_tests upstream_spine_skel_smoke_tests`: 92 passed, 563 skipped), Bevy focused nextest (`animation`: 15 passed, 28 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **TrackEntryHandle link getters aligned**: Breaking cleanup renamed the safe Rust handle equivalents of C++ `TrackEntry` link/owner getters to `get_animation_state`, `get_previous`, `get_next`, `get_mixing_from`, and `get_mixing_to`. `is_next_ready` remains unchanged because it already matches C++ `isNextReady`. Verification passed: focused core nextest (`animation_state_tests animation_state_mixing_semantics_tests`: 97 passed, 558 skipped), Bevy focused nextest (`animation`: 15 passed, 28 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **AnimationStateData getters aligned**: Breaking cleanup renamed `skeleton_data`, `default_mix`, and `get_mix_animation` to `get_skeleton_data`, `get_default_mix`, and `get_mix`, matching latest-tag C++ `getSkeletonData`, `getDefaultMix`, and `getMix(Animation&, Animation&)`. `set_mix_animation` remains the Rust name for the animation-object `setMix` overload. Verification passed: focused core nextest (`animation_state_tests`: 81 passed, 574 skipped), Bevy focused nextest (`animation`: 15 passed, 28 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Runtime constraint getters aligned**: Breaking cleanup renamed IK, transform, path, physics, and slider constraint pose read helpers to latest-tag C++ getter spelling. The public solver state reads now use `get_*` names such as `get_mix`, `get_softness`, `get_bend_direction`, `get_mix_rotate`, `get_position`, `get_inertia`, and `get_time`; setter names and runtime behavior were not changed. Verification passed: focused nextest (`skeleton_tests ik_timeline_tests transform_constraint_timeline_tests slider_timeline_tests animation_state_mixing_semantics_tests`: 55 passed, 600 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Bone pose getters aligned**: Breaking cleanup renamed `Bone` local, applied, and world pose readers to latest-tag C++ getter spelling. Public reads now use `get_inherit`, `get_x` / `get_y`, `get_rotation`, `get_scale_x` / `get_scale_y`, `get_shear_x` / `get_shear_y`, `get_applied_*`, matrix getters `get_a` / `get_b` / `get_c` / `get_d`, and world getters `get_world_*`; setter names and transform behavior were not changed. Verification passed: focused nextest (`skeleton_tests oracle_scenario_parity_tests examples_pose_parity_tests render_oracle_parity_tests`: 354 passed, 301 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Slot pose getters aligned**: Breaking cleanup renamed `Slot` pose readers to latest-tag C++ getter spelling for color, dark color, attachment-name, sequence-index, deform, and applied-pose reads. Public reads now use `get_color`, `get_dark_color`, `get_attachment_name`, `get_sequence_index`, `get_deform`, and the corresponding `get_applied_*` helpers; `has_dark_color` remains predicate-style like C++ `hasDarkColor()`. Verification passed: focused nextest (`slots_tests color_timeline_tests skeleton_tests examples_pose_parity_tests oracle_scenario_parity_tests render_oracle_parity_tests`: 359 passed, 296 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Skeleton bounds/update-cache getters aligned**: Breaking cleanup renamed `Skeleton::bounds`, `bounds_with_clipping`, and `update_cache_items` to `get_bounds`, `get_bounds_with_clipping`, and `get_update_cache`, matching latest-tag C++ `getBounds(...)` and `getUpdateCache()`. Verification passed: focused nextest (`skeleton_tests render_oracle_parity_tests examples_pose_parity_tests`: 42 passed, 613 skipped), clean-target core and example checks, `cargo fmt --all -- --check`, and `git diff --check`.
* **Atlas pages/regions getters aligned**: Breaking cleanup made `Atlas.pages` and `Atlas.regions` private and added `get_pages()` / `get_regions()`, matching latest-tag C++ `Atlas::getPages()` / `getRegions()`. Repo render, Web, WGPU, and tests now enumerate through those getters. Verification passed: focused nextest (`atlas render_tests render_oracle_parity_tests`: 40 passed, 615 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **AtlasRegion getters aligned**: Breaking cleanup made `AtlasRegion` fields private and moved public inspection to latest-tag C++ getter spelling, including `get_name`, `get_page`, `get_index`, coordinate/offset/original/packed dimension getters, `get_rotate`, `get_degrees`, metadata getters, and `TextureRegion`-style UV/region-size getters. The old Rust-only public `width` / `height` fields were deleted; parsing now writes packed dimensions directly and swaps them after UV calculation for 90-degree regions like `spine-cpp`. Verification passed: focused nextest (`atlas render_tests render_oracle_parity_tests`: 40 passed, 615 skipped), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Animation getter surface aligned**: Breaking commit `4768d3f` made `Animation` fields crate-visible and added latest-tag C++-style access through `get_name`, `get_duration`, `set_duration`, `get_color`, `get_color_mut`, and `get_timelines`. Bevy runtime snapshots and asset metadata now read names through getters, and `pose_dump_scenario` uses the unified timeline iterator instead of direct timeline-vector access for diagnostics. Verification passed: focused core nextest (`model_lookup_tests json_timeline_order_tests`: 11 passed, 646 skipped), full clean-target core nextest (`655 passed, 2 skipped`), Bevy nextest (`43 passed, 0 skipped`), clean-target checks for core, examples, Bevy examples, WGPU/Web, `cargo fmt --all -- --check`, and `git diff --check`.
* **Constraint setup data accessors aligned**: Breaking commit `dd5d0db` made IK, transform, path, physics, and slider setup data fields crate-visible and added C++-style constructors plus getter/setter surfaces. Pose diagnostics and model lookup tests now use the accessor API, with focused coverage for defaults, mutable bone/property arrays, flattened setup-pose values, and slider nullable links. Verification passed: focused model lookup nextest (`2 passed, 656 skipped`), full core nextest (`656 passed, 2 skipped`), Bevy nextest (`43 passed, 0 skipped`), checks for core, core examples, Bevy examples, WGPU examples with `json`, WGPU/Web package check, `cargo fmt --all -- --check`, and `git diff --check`.
