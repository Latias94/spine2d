---
type: "Current State"
title: "Current Engineering State"
description: "Short durable summary of the active engineering state."
tags: ["engineering-memory"]
timestamp: 2026-06-20T00:00:00Z
status: "active"
---

# Current State

- Goal: 对齐 `spine2d` 与官方 `spine-runtimes` latest 4.3 tag 的运行时行为。
- Branch: local `main`; do not revert user or other agent changes if new unrelated edits appear.
- Baseline: `spine-ts-4.3.8` / commit `8e12b1250ab88c0f890849ea45aab80338cead63`；行为参考只看本地 `repo-ref/spine-runtimes/spine-cpp`。
- Last verified:
  - `SkeletonData` now preserves the latest-tag `spine-cpp` skeleton header metadata from both JSON and binary readers: `name` exists and defaults empty for string/bytes loaders, `hash` is retained (`.skel` signed 64-bit hash is stringified with zero mapped to empty), `x/y/width/height` remain unscaled like C++, `reference_scale` is scaled by loader scale, and nonessential `fps` / `images_path` / `audio_path` use C++ defaults when absent. Verification passed on 2026-06-24: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused JSON/binary header nextest, focused parser nextest (`6 passed, 634 skipped`), and full core nextest (`630 passed, 10 skipped`).
  - `SkinData` now carries the latest-tag `spine-cpp` `Skin::getColor()` state and `Skin::copySkin` public surface. Rust uses `SkinData::DEFAULT_COLOR` (`0.99607843, 0.61960787, 0.30980393, 1.0`) for newly created/default skins, JSON array-form skins parse the optional `color` field, binary named skins preserve the nonessential color, and `SkinData::copy_skin` exposes the official deep-copy composition entry point in Rust's owned-value attachment model. Verification passed on 2026-06-24: `cargo fmt --all -- --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused skin nextest (`7 passed, 632 skipped`), and full core nextest (`629 passed, 10 skipped`).
  - Removed the Rust-only public `Skeleton::find_*_index` lookup helpers for bones, slots, and typed constraints after rechecking latest-tag `spine-cpp`, which exposes object lookups (`findBone`, `findSlot`, `findConstraint<T>`) and lets callers read indices from the returned object/data. Rust still keeps private `*_index_by_name` helpers where vector indexing is needed internally. Verification passed on 2026-06-24: `cargo fmt --all --check`, no remaining code references to the removed public helpers, `cargo check -p spine2d --features json,binary,upstream-smoke`, focused skeleton finder nextest (`2 passed, 635 skipped`), and full core nextest (`627 passed, 10 skipped`).
  - Replaced Rust-only `SkeletonData` lookup helper spellings with C++-style names: tuple-returning `SkeletonData::animation(name)` was removed in favor of `find_animation(name) -> Option<&Animation>`, and `SkeletonData::skin(name)` was renamed to `find_skin(name)`, matching latest-tag `spine-cpp` `findAnimation` / `findSkin`. The pose dump's animation-data diagnostic reads `animation_index` explicitly when it needs the index for output. Verification passed on 2026-06-24: `cargo fmt --all --check`, no remaining code references to removed lookup helpers, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo check -p spine2d-bevy --examples`, full core nextest (`627 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
  - Removed the Rust-only zero-argument `Skeleton::update_world_transform()` public helper after rechecking latest-tag `spine-cpp`, which only exposes `Skeleton::updateWorldTransform(Physics)`. All repo callers now pass `Physics::None` explicitly through `update_world_transform_with_physics`, while existing physics-enabled paths continue to pass `Physics::Update` / `Pose` / `Reset` as before. Verification passed on 2026-06-24: `cargo fmt --all`, no remaining code references to `update_world_transform()`, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo check -p spine2d-bevy --examples`, full core nextest (`627 passed, 10 skipped`), and Bevy nextest (`43 passed, 0 skipped`).
  - Rechecked JSON and binary Spine version gating against latest-tag `spine-cpp` on 2026-06-24. Rust now rejects any `skeleton.spine` / binary header string that does not start with `4.3`, matching the C++ `String::startsWith(SPINE_VERSION_STRING)` check, and the JSON smoke suite now skips non-4.3 upstream examples before parsing so intentional 4.2 fixtures are no longer treated as runtime failures. Verification passed: `cargo test -p spine2d --features json,binary,upstream-smoke --lib version_tests -- --nocapture`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`627 passed, 10 skipped`), `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`), `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, `git diff --check`, and `python3 scripts/check_spine_baseline.py --verify-local`.
  - Rechecked the latest-tag `spine-cpp` additive/instant timeline classification and the remaining `AnimationStateData` / `TrackEntry` public surface against the Rust runtime on 2026-06-24. No runtime mismatch was found in this pass: Rust's timeline classification matches the C++ `Timeline::_additive` / `_instant` sets for represented timeline kinds, `EventTimeline` remains outside Rust `TimelineKind` because events are handled separately, `AnimationStateData` mix keys by animation name like C++ `AnimationPair`, and `TrackEntryHandle::set_delay` / `set_mix_duration_with_delay` already follow the official negative-delay/direct-formula branches. The active baseline docs were cleaned up instead: stale beta/latest-numbered audit files were deleted or folded into `docs/upstream-audit-4.3-latest.md`, and `scripts/check_spine_baseline.py` now validates the current latest-tag audit map. Verification passed: `python3 scripts/check_spine_baseline.py --verify-local` and `git diff --check`.
  - The last Rust-only `AnimationState` accumulated `time: Cell<f32>` field was deleted after the public `time()` getter had already been removed. `spine-cpp` has no equivalent state, and the field was only self-incremented inside `update` with no readers. Verification passed on 2026-06-24: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and full core nextest (`623 passed, 10 skipped`).
  - Added focused coverage for the C++ same-animation/unapplied replacement branch: setting the same animation again before the first apply replaces the old entry without `mixing_from`, disposes the old entry, and emits interrupt/end/dispose/start in order. Verification passed on 2026-06-24: focused nextest for `set_animation_same_unapplied_animation_replaces_without_mixing_like_cpp`, full `runtime::animation_state_tests` (`76 passed, 557 skipped`), and full core nextest (`623 passed, 10 skipped`).
  - `Animation::timeline_order` is now crate-internal, and `Animation::timelines()` exposes a C++-style unified timeline iterator for public inspection, including event timelines at the end like the official `_timelines` list. `pose_dump_scenario` now renders timeline-order diagnostics through that public iterator instead of reading the raw order field. Verification passed on 2026-06-24: `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke json_timeline_order_tests --status-level fail` (`3 passed, 627 skipped`), `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_mixing_semantics_tests --status-level fail` (`16 passed, 614 skipped`), and `cargo check -p spine2d --features json --example pose_dump_scenario`.
  - `TrackAnimationInput` was deleted. `AnimationState::set_animation` / `add_animation` now model the C++ name overloads directly with `impl AsRef<str>`, while the C++ `Animation&` overloads are exposed as explicit Rust `set_animation_ref` / `add_animation_ref` methods. This removes the private-bound overload bridge and the stale `#[allow(private_bounds)]` waiver. Verification passed on 2026-06-24: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-web -p spine2d-wgpu`, `cargo check -p spine2d-bevy --examples`, focused animation-reference preservation nextest, and focused mixing semantics nextest.
  - `AnimationState::compute_hold` was rechecked against latest-tag `spine-cpp` `computeHold` / `from`. Rust now keeps the temporary `property_ids` table alive until the end of `animations_changed`, models C++ `Timeline::getAdditive`, `Timeline::getInstant`, and `Animation::hasTimeline(ids)` through local helpers, and uses direct `1.0 - holdMix.mix()` behavior without a Rust-only clamp. The existing queued-entry promotion and `Start` event paths were also confirmed already aligned: promotion clears the old current entry's `next`, and every `Start` path marks `animations_changed`. Verification passed on 2026-06-24: `cargo fmt --all --check`, `cargo check -p spine2d --features json,binary,upstream-smoke`, and focused `runtime::animation_state_mixing_semantics_tests` (`16 passed, 614 skipped`).
  - `AnimationState` queue event ordering now matches C++ for replacement and clear paths: queued disposals are drained before the interrupt on a normal `set_animation`, and `clear_track` drains queued disposals before the mixing-from chain ends. Verification passed on 2026-06-24: focused regression tests `set_animation_discards_queued_entries_before_interrupt_like_cpp` and `clear_track_disposes_queued_entries_before_mixing_from_like_cpp`, plus `runtime::animation_state_tests` (`75 passed, 557 skipped`) and focused mixing semantics (`16 passed, 616 skipped`).
  - `AnimationState` event queue disposal now follows the C++ `EventQueue::drain` boundary: queued `End` entries notify `End`, fall through to `Dispose`, and perform automatic release unless manual disposal is enabled. Normal track completion, clear-track, same-unapplied replacement, and mixing-from completion paths now queue only `End`; queued entries discarded by `clearNext`-style paths still queue direct `Dispose`. Verification passed on 2026-06-24: `runtime::animation_state_tests` (`73 passed, 556 skipped`), `runtime::animation_state_mixing_semantics_tests` (`16 passed, 613 skipped`), focused lifecycle regressions (`3 passed, 626 skipped`), `cargo fmt --all --check`, `git diff --check`, wiki validation, and full core parity (`619 passed, 10 skipped`, `1 leaky`).
  - `spineboy_run_to_walk_mix0_2_t0_4` `.skel` parity is restored against direct `spine-cpp` oracle output. The existing golden was correct; Rust was crossing the two-bone IK straight-line boundary when the computed cosine landed one `f32` ULP above `1.0`. `skeleton::ik` now treats only that one-ULP overrun as an in-domain `acos` solve, and BonePose world/local math uses C++-style degree/radian conversion plus fused multiply-add shaped world-matrix updates. Verification passed on 2026-06-24: focused `.skel` spineboy oracle regressions, focused IK/skeleton tests, focused additive spineboy scenarios, `cargo fmt --all --check`, `git diff --check`, and `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`).
  - `scripts/spine_cpp_lite_render_oracle.cpp` and `spine2d/examples/render_dump.rs` are now scenario-only; `scripts/record_oracle_render_goldens.py` and `scripts/render_parity_smoke.zsh` were aligned to the same `--set` / `--step` command surface, and the render goldens were re-recorded. Verification passed on 2026-06-24: `cargo check -p spine2d --example render_dump --features json,binary,upstream-smoke`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke render_oracle_parity_tests --no-fail-fast --status-level fail` (`4 passed, 625 skipped`), `./scripts/render_parity_smoke.zsh`, `cargo fmt --all --check`, `git diff --check`, and `python3 scripts/check_spine_baseline.py --verify-local`.
  - The old one-shot `spine2d/examples/pose_dump.rs` example was deleted. Pose oracle work now uses `pose_dump_scenario` for single-track, mixing, and multi-track samples, matching the command-stream C++ oracle surface. Verification passed on 2026-06-24: `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `zsh -n scripts/run_spine_cpp_lite_oracle.zsh`, a `pose_dump_scenario --set 0 run 1 --step 0.1` JSON smoke, and a repo search showing no live `pose_dump.rs` references remain.
  - `pose_dump_scenario` now supports the same `--entry-event-threshold` and `--entry-alpha-attachment-threshold` TrackEntry commands as the C++ pose oracle and `render_dump`; the pose and render oracle scenario CLIs now cover the same threshold control surface. Verification passed on 2026-06-24: `cargo build -p spine2d --example pose_dump_scenario --features json,binary,upstream-smoke`, a `pose_dump_scenario --entry-event-threshold 0.5 --entry-alpha-attachment-threshold 0.6 --step 0.1` JSON smoke, `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, and `git diff --check`.
  - Bevy skeleton setup-pose command naming now follows the core/C++ `setup_pose` shape. `SpineSkeletonCommand::reset_to_setup_pose` and `SpineSkeletonCommandKind::ResetToSetupPose` were deleted in favor of `SpineSkeletonCommand::setup_pose` and `SpineSkeletonCommandKind::SetupPose`. Verification passed on 2026-06-24: `rg -n "ResetToSetupPose|reset_to_setup_pose" spine2d-bevy` returned no code/example matches, `cargo check -p spine2d-bevy --examples`, and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`).
  - The unused `Error::InvalidTrackIndex` enum variant was deleted after the public track APIs had already moved away from Rust `Result` returns toward C++ assert/no-op semantics. `Error::InvalidValue` remains because JSON parsing still uses it for invalid export data such as bad sequence modes. Verification passed on 2026-06-24: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all --check`, `git diff --check`, and a repo search showing no `InvalidTrackIndex` references remain.
  - The Rust-only inherent `Atlas::from_str` alias was deleted as a breaking public-surface cleanup. `Atlas::parse` remains the explicit crate helper and `impl FromStr for Atlas` remains the idiomatic trait path; repo callers now use `Atlas::parse`. Verification passed on 2026-06-24: no `Atlas::from_str` or `should_implement_trait` references remain, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, and `cargo check -p spine2d-wgpu -p spine2d-web` with only the existing `block v0.1.6` future-incompatibility warning.
  - `MixDirection` was narrowed from public to crate-visible because it is an internal mix-in/mix-out dispatch helper, not a latest-tag `spine-cpp` public type. Verification passed on 2026-06-24: `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all --check`, and `git diff --check`.
  - The standalone `apply_animation` helper is now test-only, so the non-test crate surface keeps the crate-private `apply_animation_applied` runtime helper without exporting a standalone sampling entry point. Verification passed on 2026-06-24: `cargo check -p spine2d --features json,binary,upstream-smoke`, focused runtime sampling tests (`67 passed, 562 skipped`), `cargo fmt --all --check`, and `git diff --check`.
  - `MixBlend` was narrowed to crate visibility. It remains the Rust runtime's internal bridge from C++ `MixFrom` + additive `bool`, but it is no longer part of the crate public surface because latest-tag `spine-cpp` does not expose public `mixBlend`. Verification passed on 2026-06-24: `cargo check -p spine2d --features json,binary,upstream-smoke`.
  - Rechecked `ConstraintRef` / `Skeleton::constraints()` against latest-tag `spine-cpp` and kept it: C++ still exposes `Skeleton::getConstraints()`, so the Rust typed enum wrapper remains the safe analogue for the unified constraint view. Verification passed on 2026-06-24: `cargo check -p spine2d --features json,binary,upstream-smoke` and focused `runtime::skeleton_tests`.
  - The Rust-only `TrackEntrySettings` value object was removed from core `spine2d`. Core runtime users now configure entries through `TrackEntryHandle` setters, while Bevy owns its `SpineTrackEntrySettings` command helper locally. Verification passed on 2026-06-24: core check, focused `runtime::animation_state_mixing_semantics_tests` (`16 passed, 613 skipped`), Bevy examples check, Bevy nextest (`43 passed, 0 skipped`), `cargo fmt --all --check`, and `git diff --check`.
  - Post-cleanup verification passed on 2026-06-24: `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`), `cargo check -p spine2d --examples --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, `git diff --check`, baseline verification, and wiki validation. The WGPU/Web check still reports the existing `block v0.1.6` future-incompatibility warning.
  - `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`) remained green after the render-oracle cleanup; no runtime behavior changed.
  - `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`) passed on 2026-06-24 after trimming the last single-use draw-order / attachment helper shells: `slot_pose_ref` was removed, `set_draw_order_to_setup` / `set_draw_order_from_slice` were inlined away, and `animation.rs` now writes draw-order targets directly. The observable behavior stayed green; the internal helper surface is just flatter now.
  - `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`) passed on 2026-06-24 after simplifying `AnimationState` draw-order dispatch: `draw_order_folder_out` was removed, `draw_order_out` was collapsed from `Option<bool>` to `bool`, and the runtime now derives folder `out` directly from `MixDirection`. The observable behavior stayed green; the state surface is just thinner.
  - `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`) passed on 2026-06-24 after removing the last single-use `entry_for_handle_mut` helper and inlining the remaining `MixFrom::from_blend` mapping at call sites. The runtime behavior stayed the same; the helper surface is just smaller now.
  - `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`) passed on 2026-06-24 after inlining the last single-use `previous_entry_for` helper and flattening the current-entry/current-mixing-from timeline mode mapping inside `AnimationState`. The observable parity surface stayed the same; the module is just thinner and easier to audit now.
  - `cargo fmt --all --check`, `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` (`619 passed, 10 skipped`), and `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` (`43 passed, 0 skipped`) passed on 2026-06-24 after a small `AnimationState::apply_entry_pose` signature cleanup. The current-entry path now passes the fixed `MixDirection::In` at the call site instead of threading a one-off parameter through the helper, so the refactor stays close to the official C++ branch shape without changing behavior.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_mixing_semantics_tests --no-fail-fast --status-level fail` passed with `15 passed, 609 skipped` on 2026-06-24 after restoring C++-style linear `TrackEntry::mix()` pass-through for negative mix ratios. `TrackEntry::mix_percent()` now returns the raw linear ratio when the interpolation is linear, while non-linear curves still clamp after interpolation.
  - `cargo fmt --all --check` and `cargo check -p spine2d --features json,binary,upstream-smoke` passed on 2026-06-24 after removing the crate-level `#![allow(dead_code)]` masks from `spine2d/src/geometry.rs` and `spine2d/src/binary_tests.rs`. Those modules now compile cleanly without a blanket dead-code waiver.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::upstream_event_timeline_tests::event_timeline_libgdx_upstream_tests --no-fail-fast --status-level fail` passed with `1 passed, 622 skipped` on 2026-06-24 after deleting the runtime-only `collect_events_for_tests` bridge and exposing the shared event collector to the runtime test module directly. `cargo fmt --all --check` and `cargo check -p spine2d --features json,binary,upstream-smoke` also passed.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `613 passed, 10 skipped` on 2026-06-24 after aligning `AnimationState::update` with the C++ outer `updateMixingFrom` cleanup path. Rust now consumes `update_mixing_from`'s completion return, clears the current entry's remaining `mixing_from` chain, disconnects the first outgoing entry's `mixing_to`, and queues End/Dispose for the remaining chain like C++ does when all mixing-from entries have completed. Focused `completed_hold_mix_chain_detaches_current_mixing_from`, `runtime::animation_state_mixing_semantics_tests` (`14 passed, 609 skipped`), `runtime::animation_state_tests` (`69 passed, 554 skipped`), Bevy nextest (`43 passed, 0 skipped`), core/Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning during WGPU/Web check.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `612 passed, 10 skipped` on 2026-06-24 after aligning `AnimationState::apply_mixing_from_pose` with C++ `AnimationState::applyMixingFrom` for non-hold `DrawOrderTimeline` mix-out. When `drawOrder == false` and `mixFrom == Current`, Rust now skips the timeline before adding to `total_alpha`, so an outgoing draw-order-only entry does not keep the mixing chain alive after its mix completes. Focused `draw_order_current_mix_out_does_not_keep_mixing_from_alive`, `runtime::animation_state_mixing_semantics_tests`, `runtime::animation_state_tests`, Bevy nextest, core/Bevy/WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning during WGPU/Web check.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `611 passed, 10 skipped` on 2026-06-24 after aligning `AnimationStateData::set_mix`, `AnimationState::set_animation`, and `AnimationState::add_animation` with latest-tag C++ direct-return/assert semantics. Missing animation names now panic like the C++ name overload assertions instead of returning `Result`; Bevy missing-animation commands were updated to expect the panic and preserve prior entry settings. `runtime::animation_state_tests` passed with `69 passed, 552 skipped`; `spine2d-bevy` passed with `43 passed, 0 skipped`; `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, Python oracle recorder byte-compile, zsh runner syntax checks, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning during WGPU/Web check.
  - TrackEntry queue/link behavior now follows C++ lifecycle more closely: promoting a queued entry clears the previous current entry's `next`, empty-track queued starts mark `animations_changed`, `set_delay` again ignores negative values, handle neighbor reads filter disposed targets, and manual disposal preserves disposed queued links until explicit `dispose_track_entry`.
  - `repo-ref/spine-runtimes` is now detached at the pinned `spine-ts-4.3.8` commit. Latest-tag C++ `TrackEntry` exposes `getAdditive/setAdditive` and `getMixInterpolation/setMixInterpolation`; it does not expose `holdPrevious` or public `mixBlend`. The Rust/Bevy/scenario surface has been realigned accordingly: `hold_previous`, `mix_blend`, `set_mix_blend`, `set_hold_previous`, `with_mix_blend`, `with_hold_previous`, `EntryMixBlend`, `--entry-mix-blend`, and `--entry-hold-previous` were removed; additive scenarios now use `set_additive` / `with_additive` / `--entry-additive`, and mix interpolation uses `MixInterpolation` / `--entry-mix-interpolation`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `610 passed, 10 skipped` on 2026-06-23 after the latest-tag TrackEntry API cleanup. Focused mixing semantics and render oracle scenario tests also passed, including the tank additive overlay scenarios that previously failed.
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped` on 2026-06-23 after Bevy runtime state/settings moved from `mix_blend` / `hold_previous` to `additive` / `mix_interpolation`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `617 passed, 10 skipped` on 2026-06-24 after fixing negative-track-time sentinels in `AnimationState::set_animation`, `update_mixing_from`, and `TrackEntry::wasApplied`-style readiness. C++ now only treats `-1` as the unapplied sentinel; negative `track_time` no longer makes a previously-applied entry look fresh. Focused regressions `negative_track_end_clears_when_track_last_passes_it`, `completed_hold_mix_chain_with_negative_track_time_still_detaches`, and `set_animation_same_animation_with_negative_track_time_still_mixes_from_applied_entry` passed, along with `cargo fmt --all --check` and the full core gate.
  - `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, Python oracle recorder byte-compile, zsh runner syntax checks, and C++ pose/render oracle smoke runs with `--entry-additive 1` passed. `block v0.1.6` still emits the existing future-incompatibility warning during WGPU/Web check.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `610 passed, 10 skipped` on 2026-06-23 after restoring `TrackEntry::mix_interpolation` parity with C++ `TrackEntry::mix()` and reintroducing the C++ `keepHold` hold-chain state bit: the Rust runtime now stores `MixInterpolation` on each track entry, exposes `mix_interpolation` / `set_mix_interpolation` / `with_mix_interpolation`, and uses the chosen interpolation when computing mix percentage while preserving hold bits when the upstream chain requests it. Focused interpolation/hold tests, `cargo check -p spine2d --features json,binary,upstream-smoke`, `cargo fmt --all --check`, and `git diff --check` passed.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `608 passed, 10 skipped` on 2026-06-23 after aligning the remaining `mixDuration == 0` / `to_mix_duration == 0` checks with C++: zero mix durations keep the single-frame mix path, while negative mix durations stay stored directly on queued entries and the activation regression `queued_negative_mix_duration_is_preserved_on_activation` now passes. Focused mix-duration tests passed individually; `cargo check -p spine2d --features json,binary,upstream-smoke`, Bevy examples, WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `607 passed, 10 skipped` on 2026-06-23 after aligning reverse playback event queueing with C++: reverse current and mixing-from entries pass no event collection buffer, so EventTimeline events are not queued during reverse playback while complete handling still follows `queueEvents`. Focused reverse/AnimationState tests passed with `67 passed, 550 skipped`; `cargo check -p spine2d --features json,binary,upstream-smoke`, Bevy examples, WGPU/Web checks, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `607 passed, 10 skipped` on 2026-06-23 after splitting Rust event handling back toward the C++ layering: `collect_events` now mirrors `EventTimeline::apply` only, while `apply_entry_events_and_complete` mirrors `AnimationState::queueEvents` ordering/filtering and uses C++ signed `%` behavior for `trackLastWrapped`. Focused queue/EventTimeline tests passed with `9 passed, 608 skipped`; `runtime::animation_state_tests upstream_event_timeline_tests` passed with `68 passed, 549 skipped`; Bevy examples, WGPU/Web checks, `cargo fmt --all --check`, `git diff --check`, and engineering wiki validation passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `606 passed, 10 skipped` on 2026-06-23 after aligning looping complete zero-duration checks with C++ exact `duration == 0` / `duration != 0`, removing the Rust-only epsilon zero-duration classification. Focused loop/event tests passed with `7 passed, 609 skipped`, `runtime::animation_state_tests` passed with `66 passed, 550 skipped`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `605 passed, 10 skipped` on 2026-06-23 after aligning non-looping complete detection with C++ direct comparisons: `animationTime >= animationEnd && animationLast < animationEnd`, without epsilon padding. Focused event/complete tests passed with `5 passed, 610 skipped`, `runtime::animation_state_tests` passed with `65 passed, 550 skipped`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `604 passed, 10 skipped` on 2026-06-23 after removing remaining epsilon comparisons from `TrackEntry::animation_time()`: loop duration uses exact `duration == 0`, and non-looping `animation_end >= animation.duration` now follows the C++ direct comparison. Focused exact-duration tests passed with `2 passed, 612 skipped`, `runtime::animation_state_tests` passed with `64 passed, 550 skipped`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `603 passed, 10 skipped` on 2026-06-23 after aligning looping `TrackEntry::track_complete()` with C++ `(int)(trackTime / duration)` truncation semantics for negative track time. Focused loop-time tests passed with `2 passed, 611 skipped`, `runtime::animation_state_tests` passed with `63 passed, 550 skipped`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `602 passed, 10 skipped` on 2026-06-23 after aligning looping `TrackEntry::animation_time()` with C++ `MathUtil::fmod` semantics: exact loop boundaries return `animation_start`, and negative track time keeps the signed remainder behavior instead of normalizing into `[0, duration)`. Focused animation-start/end tests passed with `5 passed, 607 skipped`, `runtime::animation_state_tests` passed with `62 passed, 550 skipped`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_data --no-fail-fast --status-level fail` passed with `8 passed, 603 skipped` on 2026-06-23 after removing the stale `Result` wrapper from `AnimationStateData::set_mix_animation` / `get_mix_animation`; those now match C++ `setMix(Animation&, Animation&)` returning `void` and `getMix(Animation&, Animation&)` returning `float` directly. `cargo fmt --all --check` and `git diff --check` passed.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `600 passed, 10 skipped` on 2026-06-23 after breaking commit `bdaa7db` aligned `AnimationState::set_animation` / `add_animation` and `TrackEntryHandle::set_animation` with C++ `Animation&` semantics. Name inputs still resolve through `SkeletonData`; animation-object inputs preserve the caller-provided animation value and use animation identity for the C++ same-animation replacement branch. `AnimationStateData` mix storage now keys by animation name like C++ `AnimationPair::operator==`, while `set_mix` still rejects missing names. `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke track_entry_set_animation --no-fail-fast --status-level fail` passed with `2 passed, 607 skipped` on 2026-06-23 after adding `TrackEntryHandle::set_animation` in commit `578b3bf`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `599 passed, 10 skipped` on 2026-06-23 after the initial TrackEntry animation-setter slice. That earlier slice was later corrected because C++ `TrackEntry::setAnimation(Animation&)` stores the supplied animation reference directly rather than canonicalizing by name. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests --no-fail-fast --status-level fail` passed with `57 passed, 550 skipped` on 2026-06-23 after removing `TrackEntrySnapshot::animation_index` and switching event expected rows to `animation_name`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after the snapshot animation-index cleanup. `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - After removing Bevy `SpineTrackState::animation_index` in commit `2211af7`, `cargo clean -p spine2d-bevy && cargo test -p spine2d-bevy --no-run`, `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23. `block v0.1.6` still emits the existing future-incompatibility warning.
  - Bevy runtime test execution for commit `2211af7` did not complete: `cargo nextest run -p spine2d-bevy ...`, `cargo test -p spine2d-bevy runtime_state_snapshot...`, and direct `target/debug/deps/spine2d_bevy-... --help` all hung before the test harness produced output. No leftover test processes remained after interrupting those attempts.
  - `cargo test -p spine2d --features json,binary,upstream-smoke state_time_scale_scales_update_and_queue_progression -- --nocapture` and `cargo test -p spine2d --features json,binary,upstream-smoke update_accepts_negative_delta_like_cpp -- --nocapture` passed on 2026-06-23 after removing the Rust-only `AnimationState::time()` getter. `cargo nextest run -p spine2d --features json,binary,upstream-smoke update_accepts_negative_delta_like_cpp --no-fail-fast --status-level fail` also passed with `1 passed, 606 skipped`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after removing `AnimationState::time()`. `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped`; `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after removing `TrackEntry::animation_index` and `TrackEntry::total_alpha`; `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped`; `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke animation_state_data_default_mix_is_directly_stored_and_used_as_fallback animation_state_data_named_mix_overrides_default_mix animation_state_data_animation_mix_accessors_match_name_mix_storage animation_state_data_clear_resets_default_and_animation_mixes animation_state_data_rejects_unknown_animations --no-fail-fast --status-level fail` passed with `5 passed, 602 skipped` on 2026-06-23 after deleting `AnimationStateData::get_mix`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after deleting the name-based mix getter.
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped` on 2026-06-23 after deleting the name-based mix getter.
  - `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the name-based mix getter cleanup. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo check -p spine2d --features json,binary,upstream-smoke` passed on 2026-06-23 after deleting `with_track_entry` / `with_queued_track_entry`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after deleting `with_track_entry` / `with_queued_track_entry`.
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped` on 2026-06-23 after moving Bevy test helpers to `current()` / `next()` / `with_entry()`.
  - `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the track-entry helper cleanup. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after deleting `tracks_len` and `track_snapshots`.
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped` on 2026-06-23 after switching Bevy runtime-state sync to `tracks()` plus state-bound entry reads.
  - `cargo check -p spine2d-bevy --examples`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the snapshot API cleanup. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::animation_state_data_named_mix_overrides_default_mix runtime::animation_state_tests::animation_state_data_clear_resets_default_and_animation_mixes runtime::animation_state_tests::animation_state_data_rejects_unknown_animations --no-fail-fast --status-level fail` passed with `3 passed, 604 skipped` on 2026-06-23 after removing Rust-only mix pair query/removal APIs.
  - `cargo nextest run -p spine2d-bevy mix_set_and_clear_commands_update_state_data --no-fail-fast --status-level fail` passed with `1 passed, 42 skipped` on 2026-06-23 after removing the Bevy `remove_mix` command.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after the mix API cleanup.
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped` on 2026-06-23 after the mix API cleanup.
  - `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo check -p spine2d-bevy --examples`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the mix API cleanup. `block v0.1.6` still emits the existing future-incompatibility warning.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::track_entry_handle_reads_current_and_queued_entries --no-fail-fast --status-level fail` passed with `1 passed, 606 skipped` on 2026-06-23 after adding `TrackEntryHandle::with_entry`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `597 passed, 10 skipped` on 2026-06-23 after adding `TrackEntryHandle::with_entry`.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the handle read accessor slice.
  - `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the handle read accessor slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::track_entry_handles_are_bound_to_their_animation_state --no-fail-fast --status-level fail` passed with `1 passed, 605 skipped` on 2026-06-23 after binding `TrackEntryHandle` to its owning `AnimationState`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `596 passed, 10 skipped` on 2026-06-23 after binding `TrackEntryHandle` to its owning `AnimationState`.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the state-bound handle slice.
  - `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the state-bound handle slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::animation_state_data_animation_mix_accessors_match_name_mix_storage --no-fail-fast --status-level fail` passed with `1 passed, 604 skipped` on 2026-06-23 after the `AnimationStateData` animation-reference mix accessor slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `595 passed, 10 skipped` on 2026-06-23 after the `AnimationStateData` animation-reference mix accessor slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the `AnimationStateData` animation-reference mix accessor slice.
  - `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the `AnimationStateData` animation-reference mix accessor slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_tests::animation_state_data_skeleton_data_accessor_exposes_bound_skeleton --no-fail-fast --status-level fail` passed with `1 passed, 603 skipped` on 2026-06-23 after the `AnimationStateData::skeleton_data` accessor slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `594 passed, 10 skipped` on 2026-06-23 after the `AnimationStateData::skeleton_data` accessor slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the `AnimationStateData::skeleton_data` accessor slice.
  - `cargo check -p spine2d-bevy`, `cargo check -p spine2d-wgpu -p spine2d-web`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the `AnimationStateData::skeleton_data` accessor slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `593 passed, 10 skipped` on 2026-06-23 after the `AnimationState::tracks` slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the `AnimationState::tracks` slice.
  - `cargo check -p spine2d-bevy`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the `AnimationState::tracks` slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `593 passed, 10 skipped` on 2026-06-23 after the manual TrackEntry disposal slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the manual TrackEntry disposal slice.
  - `cargo check -p spine2d-bevy`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the manual TrackEntry disposal slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `592 passed, 10 skipped` on 2026-06-23 after the `AnimationState::disable_queue` / `enable_queue` slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the `AnimationState::disable_queue` / `enable_queue` slice.
  - `cargo check -p spine2d-bevy`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the `AnimationState::disable_queue` / `enable_queue` slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `591 passed, 10 skipped` on 2026-06-23 after the `AnimationState::current` / `data` accessor slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the `AnimationState::current` / `data` accessor slice.
  - `cargo check -p spine2d-bevy`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the `AnimationState::current` / `data` accessor slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `589 passed, 10 skipped` on 2026-06-23 after the `AnimationState::apply` return-status slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23 after the `AnimationState::apply` return-status slice.
  - `cargo check -p spine2d-bevy`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the `AnimationState::apply` return-status slice.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke --no-fail-fast --status-level fail` passed with `588 passed, 10 skipped` on 2026-06-23 after the TrackEntry link/time-setter API slice.
  - `cargo nextest run -p spine2d-bevy` passed with `43 passed, 0 skipped` on 2026-06-23.
  - `cargo check -p spine2d-bevy`, `cargo fmt --all --check`, and `git diff --check` passed on 2026-06-23 after the TrackEntry link/time-setter API slice.
  - C++ pose/render oracle runners compile and run against pinned `repo-ref/spine-runtimes` with latest-tag `--entry-additive 1`; generated temp JSON validates with `python3 -m json.tool`.
  - `cargo nextest run -p spine2d --features json,binary,upstream-smoke runtime::animation_state_mixing_semantics_tests` passed with `12 passed, 608 skipped` on 2026-06-23 after latest-tag additive / mix interpolation cleanup.
  - `cargo nextest run -p spine2d-bevy --no-fail-fast --status-level fail` passed with `43 passed, 0 skipped` on 2026-06-23 after Bevy track-entry settings/state plumbing moved to `additive` / `mix_interpolation`.
- Done:
  - Confirmed `4.3.2` is not the latest 4.3 tag; current explicit baseline is `spine-ts-4.3.8`.
  - Confirmed official 4.3.4 IK uses `ScaleYMode/scaleY`, not development HEAD `uniform`.
  - Confirmed `.json` and `.skel` run-to-walk scenarios are green after refreshing stale goldens.
  - Re-recorded stale `sack_*` physics goldens against the pinned official oracle.
  - Added upstream IK demo coverage for both JSON and `.skel`.
  - Locked exact draw batching parity with official renderers via unit tests for merge/split rules and the 16-bit index limit.
  - Deleted disabled Skeleton legacy solver code in commit `fbc85eb`; post-cleanup full parity gate remains green.
  - Centralized runtime timeline dispatch in commit `73edc54`; `AnimationState` now delegates concrete `TimelineKind` application to internal helpers in `animation.rs`.
  - Centralized binary parser timeline-order registration in commit `48518a5`; JSON already uses a local order reconstruction boundary.
  - Centralized track entry settings in commit `e1e827f`; Bevy now aliases the core runtime settings value object, and queued delay/mix-duration handling follows the `spine-cpp` two-argument `setMixDuration` rule.
  - Hid `TrackEntry` fields in commit `fc1c241`; external code now reads entry state through getters instead of broad public fields.
  - Preserved `spine-cpp` delay branch behavior in commit `f36cfa7`; negative delay is special-cased without coercing non-comparable delay values.
  - Started U6 Skeleton extraction in commit `3edaa0b`; path constraint scratch storage and capacity estimation now live in private `skeleton::path`.
  - Continued U6 Skeleton extraction in commit `0dab0fb`; path attachment lookup and path world-position helpers now live in private `skeleton::path`. Generic attachment world-vertex computation intentionally remains in `skeleton.rs` because it is still shared by `Skeleton::world_vertices`.
  - Continued U6 Skeleton extraction in commit `190a119`; update-cache ordering now lives in private `skeleton::cache`, mirroring the official C++ `Skeleton::updateCache`/constraint `sort` responsibility boundary while preserving the Rust centralized constraint model.
  - Continued U6 Skeleton extraction in commit `757b2f7`; BonePose-equivalent world/local transform helpers and root/child world-transform math now live in private `skeleton::bone`, while the `Bone` type itself remains in `skeleton.rs` for now.
  - Continued U6 Skeleton extraction in commit `a37abac`; BonePose-equivalent `modifyWorld`, `modifyLocal`, child world-reset, and applied-transform decomposition now live in private `skeleton::bone`.
  - Continued U6 Skeleton extraction in commit `fc3ef3c`; the bone world-transform update entry now delegates to private `skeleton::bone`, completing the low-risk BonePose helper extraction slice.
  - Continued U6 Skeleton extraction in commit `e076419`; IK solver entry and helper routines now live in private `skeleton::ik`.
  - Continued U6 Skeleton extraction in commit `d772a9f`; transform constraint solver entry and helper routines now live in private `skeleton::transform`.
  - Continued U6 Skeleton extraction in commit `6be2f7b`; physics constraint solver entry and helper routines now live in private `skeleton::physics`.
  - Continued U6 Skeleton extraction in commit `6104586`; slider constraint solver entry and helper routines now live in private `skeleton::slider`.
  - Continued U6 Skeleton extraction in commit `5e93794`; path constraint apply entry now lives in private `skeleton::path`, and its path-only helper visibility was narrowed.
  - Continued U6 Skeleton extraction in commit `7f98a3d`; generic attachment world-vertices computation now lives in private `skeleton::vertices`, matching the official `VertexAttachment::computeWorldVertices` responsibility more closely.
  - Continued U6 Skeleton extraction in commit `b712f53`; the `Bone` runtime type now lives in private `skeleton::bone` and is re-exported from `skeleton` so the external type path stays stable.
  - Continued U6 Skeleton extraction in commit `6f56a26`; the `Slot` runtime type now lives in private `skeleton::slot` and is re-exported from `skeleton` so the external type path stays stable.
  - Continued U6 Skeleton extraction in commit `fcf3389`; IK, transform, path, physics, and slider runtime constraint types now live in their matching private modules and are re-exported from `skeleton`.
  - Continued U6 Skeleton API hardening in commit `047be09`; `Skeleton` container/state fields are now crate-visible with public accessor and setter methods aligned to the official C++ getter/setter shape.
  - Continued U6 Bone API hardening in commit `12218d2`; local pose, applied pose, active state, and world-transform fields are now crate-visible with public accessors/setters matching the official `BoneLocal`/`BonePose` shape.
  - Continued U6 Slot API hardening in commit `2643dd0`; slot pose fields are now crate-visible with public accessors/setters matching the official `SlotPose` shape, including attachment-change deform/sequence reset.
  - Continued U6 constraint API hardening in commit `0c8d8cd`; IK, transform, path, physics, and slider runtime pose fields are now crate-visible with public accessors/setters matching the official constraint pose/control shape. Physics integration internals are crate-visible only.
  - Added official-style physics movement controls in commit `ecdf83f`; `Skeleton::physics_translate`, `Skeleton::physics_rotate`, `PhysicsConstraint::translate`, and `PhysicsConstraint::rotate` now match the spine-cpp entry points and formulas.
  - Added official-style named lookup and attachment helpers in commit `9bae119`; `Skeleton` now exposes root/find bone/slot helpers, slot-name attachment lookup, and no-op-on-miss `set_attachment` semantics with source-skin-aware reset behavior.
  - Added no-clipper attachment bounds helper in commit `83df693`; `Skeleton::bounds` now computes official-style AABB over active region and mesh attachments using draw order.
  - Added official-style constraint lookup helpers in commit `fed0975`; the current public surface exposes by-name find/mut helpers for IK, transform, path, physics, and slider constraints, while direct index lookup is private because C++ exposes `findConstraint<T>` rather than public `findConstraintIndex` helpers.
  - Added official-style setup pose split APIs in commit `955cc27`; `Skeleton` now exposes `setup_pose`, `setup_pose_bones`, and `setup_pose_slots`, and empty setup attachments reset `sequenceIndex` like `spine-cpp`.
  - Removed the legacy `Skeleton::set_to_setup_pose` alias in breaking commit `c385349`; internal tests, examples, and backend callers now use `setup_pose` directly.
  - Added clipping-aware bounds in commit `43c5503`; `Skeleton::bounds_with_clipping` now matches the official optional `SkeletonClipping` bounds overload while `bounds()` remains the no-clipper default.
  - Aligned Skeleton world/skin controls in breaking commit `aec70e4`; wind/gravity/time/update now use direct C++ assignment semantics, component-level wind/gravity accessors exist, `set_skin(Some(missing))` no-ops, and `Error::UnknownSkin` was removed.
  - Removed the useless `Skeleton::set_skin` `Result` wrapper in breaking commit `ae1ab99`; callers now use the no-return setter directly.
  - Aligned the current skin accessor in breaking commit `ea3d166`; `Skeleton::skin()` now returns `Option<&SkinData>` like C++ `getSkin()`, while `skin_name()` exposes the stored skin name.
  - Added official-style update-cache inspection in commit `f0903f1`; `Skeleton::update_cache_items()` exposes a read-only typed view over the solver cache, matching C++ `getUpdateCache()` without exposing mutable internals.
  - Added official-style ordered constraint inspection in commit `295c836`; `Skeleton::constraints()` returns typed `ConstraintRef` entries sorted by global constraint order, covering the safe Rust analogue of latest-tag C++ `Skeleton::getConstraints()`.
  - Added Bone/BonePose transform helper surface in commit `c20ab80`; `Bone::child_indices`, parent-space point transforms, local/world rotation transforms, and `rotate_world` now cover the remaining read-only/math helper shape from C++ `Bone`/`BonePose`.
  - Added BonePose update/local helper surface in commit `b2cadd4`; `Skeleton` now exposes single-bone world/local transform update, validation, and local/world modification markers without exposing C++'s raw update counter.
  - Fixed the public BonePose local-update wrapper in commit `e3e96c0`; `Skeleton::update_bone_local_transform` and validation now keep the bone world epoch current like C++ `BonePose::updateLocalTransform`.
  - Added `Bone::is_y_down/set_y_down` in commit `d374ddf`; `Skeleton::scale_y()` and world transforms now honor the C++-style global Y-down switch, while the default stays false to preserve the repo's Y-up oracle baseline.
  - Removed hidden `Skeleton::debug_update_cache` in commit `71ddc60`; debugging callers now format the typed `update_cache_items()` view locally instead of keeping a Rust-only public compatibility helper.
  - Renamed hidden `Skeleton::slot_vertex_attachment_world_vertices` to documented `slot_attachment_world_vertices` in breaking commit `9ea3c43`, matching the C++ `VertexAttachment::computeWorldVertices` role without keeping the old hidden Rust-only name.
  - Separated applied draw order in commit `26c4709`; `Skeleton::draw_order()` now reports the applied buffer while `draw_order_pose()` keeps the unconstrained pose, matching C++ `DrawOrder` pose/applied semantics closely enough for slider-driven draw-order timelines.
  - Separated applied slot pose from the unconstrained slot pose in commit `9147966`; slider-driven slot, attachment, sequence, and deform timelines now target the applied `SlotPose` when C++ `Animation::apply(..., appliedPose=true)` would, and rendering/oracle/world-vertex reads use the applied pose.
  - Removed the Rust-only `Slot::set_attachment_name` helper in breaking commit `068263d`; runtime callers must use `Skeleton::set_attachment` for name-based attachment changes so source-skin lookup and deform/sequence reset semantics stay aligned with C++.
  - Removed the Rust-only `Slot::set_bone_index` helper in breaking commit `7b83c3c`; slot-to-bone binding remains construction data plus read-only `bone_index()`, matching C++ `Slot::getBone()`.
  - Removed runtime `Slot` blend storage in breaking commit `9afff8b`; rendering now reads blend mode from `SlotData`, matching C++ `SlotData::getBlendMode()`.
  - Aligned public attachment mutation in commit `4f8b351`; `Skeleton::set_attachment`, `set_skin` attachAll paths, and animation attachment timelines now share the C++ `SlotPose::setAttachment` deform-clear rule based on vertex timeline attachment identity.
  - Added official-style `SkinData` attachment helpers in commit `b3ca6c1`; `set_attachment` grows slot storage like C++ `Skin::setAttachment`, and remove/find/list helpers are now available for runtime composition.
  - Removed the Rust-only `Skeleton::set_attachment` success return in breaking commit `7cd8d8c`; the method now matches C++ void/no-op semantics.
  - Aligned `AnimationStateData` and empty-animation mix duration setters in breaking commit `0c78468`; default mix, pair mix, `set_empty_animation`, and `set_empty_animations` now follow C++ direct assignment semantics instead of rejecting negative or non-finite durations.
  - Removed the useless `AnimationState::add_empty_animation` `Result` wrapper in breaking commit `0a4204a`; the method now returns a `TrackEntryHandle` directly and no longer rejects non-finite delay values.
  - Aligned `AnimationState::update` in commit `f381dc5`; the runtime no longer rejects negative or non-finite delta values, and Bevy no longer clamps negative `time_scale` frame deltas before calling the core runtime.
  - Aligned IK solver mix propagation in commit `65c078f`; one-bone and two-bone IK no longer skip negative or non-finite mix values before applying C++ math.
  - Aligned timeline alpha propagation in commit `64679c2`; `AnimationState` current-entry apply, IK/transform/path/physics/slider timelines, slot color/two-color helpers, and deform timelines no longer skip or clamp nonpositive alpha where `spine-cpp` applies alpha directly.
  - Historical note: commit `2f7dcb4` removed `MixInterpolation` while local `repo-ref/spine-runtimes` was on an older development checkout. That conclusion is superseded by pinned tag `spine-ts-4.3.8`, where C++ has `TrackEntry::getMixInterpolation/setMixInterpolation` and `TrackEntry::mix()` applies interpolation.
  - Aligned `AnimationState` mix and threshold boundary comparisons in commit `fce1ccc`; queued-entry activation, track end, mixing completion, mixing-from attachment/draw-order gates, attachment alpha gates, and event thresholds now use the same direct comparisons visible in `spine-cpp` instead of Rust-only epsilon padding. A direct `interruptAlpha` field rewrite was tested and rejected because existing oracle hold-mix/interrupt scenarios failed; keep the current Rust alpha compensation unless the full C++ `TimelineMode` enum is ported.
  - Historical note: commits `1a432d3`, `9bb858f`, and `e25ca6e` restored `mixBlend` / `holdPrevious` based on the stale local development checkout. They are superseded by the pinned latest tag: current public parity surface is `additive` plus `mix_interpolation`; the old TrackEntry `mix_blend` and `hold_previous` public APIs have been deleted again.
  - C++ oracle tooling now uses latest-tag `--entry-additive <0|1>` / `--entry-mix-interpolation <linear|smooth|slow-fast|fast-slow|circle>` and calls `spine_track_entry_set_additive` / `spine_track_entry_set_mix_interpolation`.
  - Added official-style TrackEntry query helpers and loop setter in commit `866b732`; `TrackEntry::is_complete`, `TrackEntry::was_applied`, `TrackEntry::is_empty_animation`, and `TrackEntryHandle::set_loop` mirror C++ `isComplete`, `wasApplied`, `isEmptyAnimation`, and `setLoop`. Bevy command helpers now provide `SpineTrackEntrySettings::with_looped` outside core.
  - Added official-style TrackEntry queue-neighbor helpers in commit `8c074f6`; `TrackEntryHandle::previous`, `TrackEntryHandle::next`, and `TrackEntryHandle::is_next_ready` mirror C++ `getPrevious`, `getNext`, and `isNextReady` using safe Rust handles.
  - Aligned TrackEntry link storage and time setters in commit `cfd01fe`; Rust now stores C++-style previous/next links explicitly, exposes `mixing_from`, `mixing_to`, `set_track_time`, and `set_mix_time`, and preserves queued-to-current link semantics after activation.
  - Aligned `AnimationState::apply` return status in commit `d39573a`; the method now returns `bool` like C++ `apply(Skeleton&)`, reporting true only when a non-delayed current track is applied.
  - Added official-style current/data accessors in commit `8a46380`; `AnimationState::current(track_index)` returns the current safe entry handle like C++ `getCurrent`, and `AnimationState::data()` pairs with `data_mut()` like C++ `getData`.
  - Added official-style event queue controls in commit `f6b88fe`; `disable_queue` and `enable_queue` expose C++ `disableQueue` / `enableQueue` semantics, including enable not draining until the next normal drain point.
  - Added optional manual TrackEntry disposal in commit `d0ada6c`; `set_manual_track_entry_disposal`, `manual_track_entry_disposal`, and `dispose_track_entry` mirror C++ manual disposal controls while keeping automatic disposal as the default.
  - Added track slots view in commit `ca658f2`; `tracks()` exposes sparse current-track handles and preserves empty slots like C++ `getTracks()`.
  - Hid `AnimationStateData::skeleton_data` behind `skeleton_data()` in commit `c2147b3`, matching C++ `getSkeletonData()` more closely and removing the public ownership leak.
  - Added `AnimationStateData::set_mix_animation` / `get_mix_animation` in commit `1f4dd8c`, covering the C++ animation-reference `setMix` / `getMix` overloads while sharing the existing name-indexed mix table. The later direct-return cleanup removed the stale Rust `Result` wrapper so these accessors now match C++ `void` / `float` shape.
  - Aligned `AnimationStateData::set_mix`, `AnimationState::set_animation`, and `AnimationState::add_animation` with latest-tag C++ direct-return/assert semantics; Bevy missing-animation commands now panic instead of silently ignoring the failed command.
  - Bound `TrackEntryHandle` to its owning `AnimationState` in commit `c30b446`; handle methods now reject cross-state misuse and `TrackEntryHandle::animation_state(&state)` mirrors the safe Rust shape of C++ `TrackEntry::getAnimationState()`.
  - Added `TrackEntryHandle::with_entry` in commit `3710686`; handles returned by set/add/current can now read current or queued entry getters directly, closer to the C++ returned `TrackEntry&` usage model.
  - Corrected `Animation&` handling after commit `578b3bf`: C++ `TrackEntry::setAnimation(Animation&)`, `AnimationState::setAnimation(Animation&)`, and `addAnimation(Animation&)` preserve the supplied animation object rather than canonicalizing by name. Rust now preserves animation-object inputs, tracks animation identity for the same-animation replacement branch, and keeps name-based inputs on the strict `SkeletonData` lookup path.
  - Removed Rust-only `AnimationStateData::pair_mix` / `remove_mix` and Bevy `SpineAnimationCommand::remove_mix` / `RemoveMix` in breaking commit `c5eb82c`; mix storage is now controlled through the official C++-style `setMix` / `getMix` / `clear` surface only.
  - Removed Rust-only `AnimationState::tracks_len` and `AnimationState::track_snapshots` in breaking commit `9c206bf`; Bevy runtime-state sync now reads `tracks()` and state-bound entry reads directly instead of a batch snapshot helper.
  - Removed Rust-only `AnimationState::with_track_entry` and `with_queued_track_entry` in breaking commit `ac9fcb8`; callers now use `current()` / `TrackEntryHandle::next()` / `TrackEntryHandle::with_entry()`.
  - Removed Rust-only `AnimationStateData::get_mix(&str, &str)` in breaking commit `472bb75`; callers now use the official C++-style animation-reference `get_mix_animation`.
  - Removed the Rust overload adapter `TrackAnimationInput`; name inputs now use `AnimationState::set_animation` / `add_animation`, while animation-object inputs use `set_animation_ref` / `add_animation_ref` to preserve the C++ `Animation&` object identity path without exposing a private-bound generic API.
  - Removed Rust-only `Atlas::from_str`; callers now use `Atlas::parse` or the standard `FromStr` trait implementation.
  - Narrowed `MixDirection` to crate visibility; public animation-state users should drive latest-tag additive/mix-out behavior through `TrackEntry` settings and `AnimationState`, not the internal timeline dispatch helper.
- In progress:
  - Autonomous spine-cpp parity hardening on local `main`, tracked by `docs/plans/2026-06-23-001-refactor-spine-cpp-parity-hardening-plan.md`; the latest AnimationState / TrackEntry surface recheck found no new runtime changes, so the next audit should move to another high-value C++ surface instead of repeating that pass.
  - Skin / Attachment latest-tag recheck is current. Attachment helper behavior remains aligned, and the newly discovered C++ `Skin::getColor` / `copySkin` surface is now represented by `SkinData::color` / `copy_skin`. This area can be treated as aligned unless a new oracle delta appears.
- Blocked:
  - None.
- Next action:
  - Pick the next high-value module by comparing Rust public/API helpers against latest-tag `spine-cpp`; likely candidates are remaining parser/export assumptions, Bevy-only compatibility wrappers, or remaining Skeleton/constraint storage details. Keep Bevy runtime tests in the verification gate for future behavior-changing slices.

# Citations

- `spine-upstream.toml`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/AnimationState.cpp`
- `repo-ref/spine-runtimes/spine-cpp/include/spine/AnimationState.h`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/IkConstraint.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/IkConstraintTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/TransformConstraintTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/PathConstraintPositionTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/PathConstraintSpacingTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/PathConstraintMixTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/PhysicsConstraintTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/SliderTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/SliderMixTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/ColorTimeline.cpp`
- `repo-ref/spine-runtimes/spine-cpp/src/spine/DeformTimeline.cpp`
- `docs/parity.md`
- `spine2d/src/runtime/skeleton.rs`
- `spine2d/src/runtime/skeleton_tests.rs`
- `spine2d/src/runtime/animation.rs`
- `spine2d/src/runtime/animation_state.rs`
- `spine2d/src/json.rs`
- `spine2d/src/binary.rs`
- `spine2d/src/runtime/upstream_ik_demo_skel_tests.rs`
- `spine2d/tests/golden/oracle_scenarios_skel/spineboy_run_to_walk_mix0_2_t0_4.json`
- `docs/plans/2026-06-23-001-refactor-spine-cpp-parity-hardening-plan.md`
